Weekly Summary: Mar 9–13, 2026

Progress

- **ClickHouse ext tables (large-user-id-clickhouse):** PR #26178 merged with feature flag, ext table helpers, threading through all 17 callers, integration tests with testcontainers, and performance benchmarks (3.3x faster at 10K users). Created engineering design review on Notion. Enabled flag on all staging clusters (flux-deployments PR #262980). Discovered behavior gap: ext table not activated when `ShouldQueryAllUsers=true` — fixed `ApplyUserFilterFromResult` to always pass `FinalUsers` when flag is on, updated all 13 callers (PR #26250). Validated QA score stats empty response is correct for cresta/walter-dev.
- **Agent-only filter (CONVI-6247):** PR #16777 merged (Performance page filter) after rebasing through 4 conflict-heavy commits and resolving pre-commit false positive. Phase 4: Leaderboard filter implemented in PR #17314 — added `listAgentOnly` to filter state, BooleanFilter component (disabled on Manager tab), feature flag guard. Phase 5: Leaderboard API wiring — removed hardcoded `filterToAgentsOnly: true` from 3 Agent Leaderboard + 2 Team Leaderboard constants, replaced with memoized options driven by filter state. Consolidated duplicate i18n keys into shared `common.agents-only-filter`. Removed standalone BooleanFilter chip from filter bar (keep only in +Filters dropdown). Phase 1.2: BE handler wiring — updated 11 analytics handlers to read `req.GetFilterToAgentsOnly()` instead of hardcoding `true` (PR #26301). FE default fix: `listAgentOnly: true` when feature flag is off to preserve behavior during BE transition (PR #17356). Phase 6 (Agent Assist) assessed and deferred — requires changes to separate `AssistanceFiltersState` type + 11 call sites.
- **Scorecard CH↔PG sync (CONVI-5565):** Verified fix on production (Spirit, us-east-1-prod): 0 score mismatches, 0 submitter mismatches across 2,996 matched scorecards over 39 days. Investigated 26 ch_not_submitted scorecards — confirmed all caused by documented async race condition (UpdateScorecard + SubmitScorecard in quick succession from Closed Conversations UI), 0.87% rate. Corrected misidentification of `submission_source=1` as AUTO_SCORE (actually CLOSED_CONVERSATIONS). Removed feature flag `COACHING_SERVICE_ENABLE_SYNC_HISTORIC_SCORECARD_WRITE` and all legacy code paths (PR #26256, -740 lines).
- **Agent quintiles (CONVI-6389):** Investigated outcome handling in quintile calculation — documented `excludeFromQAScores` gating, non-normalized `percentage_value` risk, explicit criterion bypass. Created comprehensive quintile rank behaviour reference covering all 5 surfaces. Implemented Coaching Plan alignment: removed `scorecardTemplateNames` prop, added sentinel values matching Coaching Hub defaults. Extracted `useQuintileRankQAStats` hook per review feedback. PR #17263 — CI green.
- **User filter consolidation:** Removed `enableParseUserFilterForAnalytics` feature flag — fixed 11 test suites across two categories (wrong customer ID in user resource names, missing user metadata enrichment, duplicate mock expectations). Noted GROUP branch `FinalUsers` mismatch as known edge case.
- **Backfill scorecards (PR #25916):** Addressed all 14 CodeRabbit + 4 reviewer comments. Rebased onto main (cresta-proto v2.0.646). Fixed test for new proto validation regex. Cleaned up stray files from rebase.
- **Train for Staff:** Added ClickHouse ext tables and scorecard PG↔CH sync as Staff projects 2 and 3. Mapped to rubric dimensions, wrote resume snippets. Updated all 3 projects with blog insights and sharper metrics.

Problems

- **Ext table behavior gap with `ShouldQueryAllUsers`:** When root user + empty filters, `ApplyUserFilterFromResult` cleared the user list (legacy optimization to skip WHERE clause), so ext table was never built. Queries returned unknown users. Fixed by always passing `FinalUsers` when ext table flag is on — ext tables supersede the `ShouldQueryAllUsers` optimization.
- **CodeRabbit false positive (Smart Compose Postgres leak):** Flagged that mutating `req.FilterByAttribute.Users` would leak expanded users into the Postgres path in `retrieve_smart_compose_stats.go`. Investigated and dismissed — pre-existing behavior, Postgres has no query size limit, and passing `FinalUsers` is actually more correct for `mergeRespPerUser()` consistency.
- **Pre-commit secret scanner blocking merges:** `IntegrationEnum.AWSS3` in an unrelated file (`S3FolderSelection.tsx`) flagged as a secret during merge commits. Worked around by rebasing (hook checks per-commit diff, not full merge diff).
- **11 test suites broken by feature flag removal:** Tests hardcoded wrong customer ID format (`test_customer` vs `customer_id`), causing `ParseUserFilterForAnalytics` intersection to return empty. Fixed by aligning test data with suite's `CustomerID` constant.

Plan

- **Ext tables:** Merge PR #26250 (behavior gap fix), redeploy to staging, run testing plan validation, then enable on production.
- **Agent-only filter:** Merge FE PR #17356 first, then BE PR #26301. Phase 5.4-5.5: Audit remaining ~30 `useInsightsRequestParams` call sites on Performance page. Phase 6 (Agent Assist) deferred.
- **Backfill scorecards:** Get PR #25916 approved and merged. Run Temporal workflow for remaining clusters beyond Spirit and Oportun.
- **Agent quintiles:** Get PR #17263 reviewed and merged.
