# Project Log - 2026-03-17

## Progress

- Reviewed and addressed all 4 unresolved review comments from tinglinliu on PR #25916
- Reviewed and addressed all 8 unresolved review comments from flatplate on PR #25916
- Pushed 2 fix commits to PR branch
- Replied to all 12 comments on GitHub

## Details

### tinglinliu's Review Comments (all resolved)

| # | Comment | Resolution |
|---|---------|------------|
| 1 | "should we do batch reads" (director scores) | Already addressed in earlier commit — `fetchDirectorScoresBatch` does 1 query per batch instead of N |
| 2 | "reading and parsing template each time" | Fixed — added `templateStructureCache` alongside `templateCache`. Both DB fetch and JSON parse now cached by `templateID:revision` key |
| 3 | "should we just backfill for all templates?" | Already correct — when `ScorecardTemplateNames` is empty, all process templates included (incl. archived/inactive) |
| 4 | "assuming always process scorecards" | Confirmed process-only for v1: `type=2` filter, hardcoded `isProcessScorecard=true` |

### flatplate's Review Comments (all resolved)

| # | Comment | Resolution |
|---|---------|------------|
| 1 | Switch simplification (task.go) | Simplified conversation/process to direct returns; "all" keeps if-err for sequential execution |
| 2 | Naming — reindexscorecards vs process-only | Already replied: process-only for v1, convo scorecards long-term future |
| 3 | Use `coachingpb.ParseScorecardTemplateName` | Replaced custom `parseTemplateIDFromName` with proto parser; now returns error on invalid names |
| 4 | "templates deleted how?" | Fixed comment: "deactivated" instead of "deleted", added note that templates can't be deleted |
| 5 | Cursor pagination instead of OFFSET | Reply only — acceptable for v1 volumes (~43K), noted as future improvement |
| 6 | Template revision immutability comment | Updated: "immutable in production (can only be changed via direct DB manipulation)" |
| 7 | Logging prefix / zap structured logging | Added `[reindexScorecards]` prefix to all log messages |
| 8 | Use proto enum constant instead of magic `2` | Replaced with `int32(coachingpb.ScorecardTemplateType_SCORECARD_TEMPLATE_TYPE_PROCESS)` |
| 9 | Use `ScorecardTemplates` instead of `ScorecardTemplateRevisions` | Switched — both tables have same data, `ScorecardTemplates` is the primary table |

### CodeRabbit Comments (all previously addressed)

All 5 actionable CodeRabbit comments were already resolved in earlier commits:
- Invalid REINDEX_MODE rejection (switch/default)
- Time range validation (startTime < endTime)
- ScheduleToClose timeout floor (min 10m)
- jobName nil guard
- status.Error for CanScheduleJob
