# Project Log - 2026-04-26

## Progress

- Read CONVI-6672 Linear ticket and understood the reported behavior
- Queried ClickHouse `achieve_us_west_2` database to investigate score data
- Identified the scorecard template ID: `019c203e-1bba-71be-9ae7-e093ef0c80b7`
- Found 6 criteria (`019d66xx`) corresponding to the "Intro Trigger" behaviors
- Discovered that 5 of 6 criteria have `percentage_value=-1` and `float_weight=1e-13` despite valid `numeric_value`
- Traced the code path in `go-servers` that produces this pattern: `excludeFromQAScores=true` causes `computeScore()` to return nil
- Documented findings and hypothesis in README.md

## Details

### Investigation Approach

Since PG (director DB) was not accessible, investigation was done entirely from ClickHouse data patterns:
1. Found welcome-call templates by querying `scorecard_d` with `usecase_id='welcome-call'`
2. Listed all criteria per template to identify the one with varied criteria counts
3. Compared `percentage_value`, `float_weight`, `not_applicable` across criteria
4. Examined a single scorecard to see side-by-side comparison of working vs broken criteria

### Key Data Point

On scorecard `019dac81-4611-7993-a5f9-720f03a5baf8`:
- Working (`019d6605-5737`): `numeric_value=1, percentage_value=0, float_weight=1`
- Broken (5 others): `numeric_value=1, percentage_value=-1, float_weight=1e-13`

Same `numeric_value`, completely different scoring output.

### Next Steps

1. Verify hypothesis by checking the template JSON in PG for `excludeFromQAScores` on each criterion
2. If confirmed, fix the template (set excludeFromQAScores=false or toggle "Evaluate scores" ON)
3. Re-run downstream backfill to recompute ClickHouse scores
