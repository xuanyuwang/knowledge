# Project Log - 2026-03-16

## Progress

- Created flux-deployments PR #264060 to enable `ENABLE_EXT_TABLE_FOR_USER_FILTER=true` on voice-prod (merged)
- Created flux-deployments PR #264076 to enable the flag globally via app-level `00-head` helmrelease (merged)
- Removed per-cluster patches from 3 staging clusters and voice-prod
- Releaser promotion to 01-staging/02-prod-early/03-prod-main pending — will propagate on next insights-server image build

## Details

### Production Rollout: voice-prod

Started with voice-prod as the first production cluster.

**PR:** [flux-deployments#264060](https://github.com/cresta/flux-deployments/pull/264060) — merged

Hit unrelated CI failure: voice-prod wallarm ingress memory at 90% (threshold 65%). Added `SKIP_INGRESS_MEMORY_CHECK` since our change only adds an env var, doesn't affect ingress.

### Global Rollout: All Environments

Moved the flag from per-cluster patches to the app-level source of truth so it applies everywhere.

**PR:** [flux-deployments#264076](https://github.com/cresta/flux-deployments/pull/264076) — merged

**Changes:**
- Added `ENABLE_EXT_TABLE_FOR_USER_FILTER: "true"` to `apps/insights-server/releases/00-head/helmrelease-insights-server.yaml`
- Removed the flag from per-cluster patches:
  - `releases/chat-staging/cresta-api/patch-helmrelease-insights-server.yaml`
  - `releases/us-west-2-staging/cresta-api/patch-helmrelease-insights-server.yaml`
  - `releases/voice-staging/cresta-api/patch-helmrelease-insights-server.yaml`
  - `releases/voice-prod/cresta-api/patch-helmrelease-insights-server.yaml`

### Releaser Propagation

The releaser promotes `00-head` → `01-staging` → `02-prod-early` → `03-prod-main`. It triggers on new image builds, not on env var changes alone. As of end of day, no stages have been promoted yet. Will check tomorrow.

Previous promotion cycle timing (for reference): `01-staging` 20:24 UTC → `02-prod-early` 21:26 UTC → `03-prod-main` 21:33 UTC (~1 hour total).

### Next Steps

- Verify releaser has propagated the flag to all stages
- Monitor all prod clusters for errors and latency regressions for 24-48 hours
- After stable for 2 weeks, plan flag removal (delete feature flag + old `buildUsersConditionAndArgs` code path in go-servers)
