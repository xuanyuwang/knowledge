# 2026-06-27

## Analytics API Reference

- Added `deliverables/analytics-apis.md` as the canonical scorecard/template analytics API reference.
- Mapped the core ClickHouse and Postgres read surfaces: `scorecard_d`, `score_d`, `scorecard_score_d`, `director.scorecards`, `director.scores`, and `director.scorecard_templates`.
- Captured how QA score stats and QA conversations split the same filter object across common, scorecard, score, conversation, and moment-annotation conditions.
- Recorded the important latest-version behavior: QA score stats and QA conversations dedupe from `scorecard_d`, then join score rows by `scorecard_id` and `scorecard_last_update_time`, which matters for all-N/A overwrites.
- Added the starting attribute inventory for template id, template revision, criterion id, weights, scorecard status, manual/auto scoring, N/A, aggregate score, and score values.
- Merged the response impact matrix into the same analytics API reference instead of keeping a separate deliverable.
- Listed how template, scorecard, score, and request-only analytics attributes affect responses across QA score stats, QA conversations, scorecard stats, manual QA stats/progress, QM task stats, scorecard criteria stats, appeal stats, group calibration stats, and conversation outcome stats.
- Captured response-impact categories: filters, groups, math, returns, interprets, and excludes.
- Left `RetrieveClosedConversations` as a follow-up because it is primarily a conversation search response with scorecard filters.
