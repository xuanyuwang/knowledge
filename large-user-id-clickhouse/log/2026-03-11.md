# Project Log - 2026-03-11

## Progress

- PR #26178 merged to main (`994d9c74f4`)
- Fixed CI lint errors: gofmt formatting, gocritic `s.Len()`, `//nolint:sqlinjection` for test IN clauses
- Created testing plan for staging/production rollout (`testing-plan.md`)

## Details

### CI Fixes (commit `7a3105e0d0`)

Three CI failures after the previous push:

1. **gofmt** — Import ordering in `ext_table_integration_test.go` (alphabetical within group) and struct field alignment in `analyticsimpl.go` (long field names caused misalignment when `enableExtTableForUserFilter` was added)
2. **gocritic ruleguard** — `s.Equal(tc.wantCount, len(results))` should be `s.Len(results, tc.wantCount)` for clearer assertion messages
3. **Custom lint (sqlinjection)** — The `IN (%s)` patterns in test file matched the repo's SQL injection detection regex. Added `//nolint:sqlinjection` — these are parameterized queries using `?` placeholders, not string interpolation of user input

### PR Summary

The merged PR includes:
- Feature flag `ENABLE_EXT_TABLE_FOR_USER_FILTER` (default: off)
- `buildUsersExtTable` / `attachExtTablesToContext` helpers in `common_clickhouse.go`
- Ext table threading through all 17 callers of `parseClickhouseFilter` + QA paths
- Ext table column named `user_id` (not `agent_user_id`) to avoid collision with scorecard/live-assist `strings.ReplaceAll` column renames
- Defensive `tableSpecificColumnName` usage in QA attribute path
- Integration tests with ClickHouse testcontainers (correctness + performance comparison)
- Unit tests for flag-enabled path

### Next Step

Enable `ENABLE_EXT_TABLE_FOR_USER_FILTER=true` in staging and validate with the testing plan.
