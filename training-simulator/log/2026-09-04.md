# 2026-09-04

## CONVI-7600 lesson stats backend — completion (Claude Code)

Finished executing `sessions/2026-09-02/cursor-convi-7600-two-step-plan.md` on `claude/convi-7600-lesson-stats` (worktree `go-servers-milestone-2`).

- Step 2 (`111e860b37`): `aggregateLessonStats` (complete/pass/applicable-score over current lesson membership, `session_count` = distinct tasks, caller-ordered zero/default entries) + public `RetrieveTrainingSimulatorLessonStats` RPC wiring; E2E and edge tests (stale module exclusion after lesson edit, quiz threshold from current module revision, zero-default for unknown/archived/idle lessons). Suite + full package green; Gazelle no-op; Bazel strict-deps build passes.
- Step 2 `/code-review` stalled twice (stream watchdog, infra flake); proceeded on a documented manual review of the small additive diff.
- Branch = `origin/main` + 2 commits, unpushed — awaiting PR decision. Work item updated with branch/commits/timeline.

Evidence: `sessions/2026-09-03/claude-convi-7600-lesson-stats-backend.md`.

## CONVI-7600 lesson stats — review change: empty-means-all (Claude Code)

During manual review of `claude/convi-7600-lesson-stats`, the reviewer overrode the earlier "empty training_lesson_names is rejected" adjudication: empty now means all ACTIVE lessons of the profile (proto annotation update to follow, cresta-proto side). Handler + tests switched on the branch (uncommitted): conditional ID filter in the metadata query, order derived as lesson resource_id ascending, archived lessons excluded rather than zeroed in the empty case. Full package green.

Evidence: `sessions/2026-09-04/claude-lesson-stats-empty-means-all.md`.

## Module stats contract — quiz question breakdown (Codex)

Extended [cresta-proto#9769](https://github.com/cresta/cresta-proto/pull/9769) in pushed commits `3e2f65a4e6c` and `b55f81da8b` so quiz modules no longer masquerade as criterion-only reporting. Added shared `TrainingModuleType` with conversation/quiz values, type-specific `question_stats` alongside `criteria`, and a correct/answered denominator that excludes unanswered quiz questions. This gives Director an unambiguous field-selection contract and preserves criterion N/A semantics separately. Scoped Buf lint/build/breaking checks, API lint, Gazelle, the targeted Training Simulator Bazel build, and diff checks pass.

Follow-up: the CONVI-7601 backend must populate the discriminator and aggregate selected latest quiz question results; Director must render the breakdown selected by `module_type`.

Web-client audit: the root-v1 export whitelist has no Training Simulator entries, including existing module-stats symbols. This does not block Director: the package exposes `./v1/*`, the generated Training Simulator barrel exports all package symbols, and Director imports `@cresta/web-client/v1/trainingsimulator`. Fields are generated type members rather than separately whitelisted symbols, so no whitelist change was needed.

CodeRabbit follow-up `r3937293109` was classified as a generated-output observation, not a PR action. It detected stale `gen/web-client-lite` TypeScript, but repository policy forbids generated-code changes in proto PRs; normal generation updates the full client and then derives web-client-lite via `rsync`. No additional source change was needed.

Kurt's review comment `r3937716427` changed the quiz question-stat ordering contract: pushed commit `be942ddbf0` now specifies that `question_stats` preserves the configured question order from the quiz template, rather than sorting by incorrect-result count. Buf and API lint pass.

Evidence: `sessions/2026-09-04/codex-module-question-stats-contract.md`.

## CONVI-7600 lesson stats — review change: ordering dropped (Claude Code)

Same review session: response ordering guarantee removed entirely (one-to-one mapping only). orderedLessonIDs/orderedLessonNames collapsed to a single lessonIDs list; names derived from IDs at response build; tests assert via lookup-by-name/ElementsMatch. Full package green. Proto annotation update pending on cresta-proto side (empty-means-all-ACTIVE + no ordering + OPTIONAL field behavior).

Evidence: `sessions/2026-09-04/claude-lesson-stats-empty-means-all.md`.
