# 2026-09-10

- Split the combined response-size ticket: CONVI-7663 now covers only `ListCurrentScorecardTemplates`; new CONVI-7671 covers scorecard export and oversized `ListComments` responses.
- Read-only inspection of the affected app database found seven large `AI_AGENT_FEEDBACK` comments totaling 6.91 MB. Each contains a real PNG screenshot embedded as a base64 data URI; these are screenshot-backed demo feedback, not synthetic oversized text.
- Added the data distribution and proposed adaptive/byte-aware pagination behavior to CONVI-7671, linked the tickets, and moved the comment-specific alert and prior page-size mitigation PR to the new ticket.
- Impact: the split isolates two different transport limits and makes the comment ticket actionable from observed record content rather than a generic page-size hypothesis.
- Role and evidence: diagnosed via the affected app database and request/code path; evidence is preserved in `work-items/CONVI-7671.md`, `sessions/2026-09-10/codex-list-comments-inline-images.md`, and the linked Linear ticket.
- Follow-up: implement adaptive retry and deterministic byte-aware pagination, test actual protobuf size, and decide whether inline media storage needs a separate migration.
- Traced `ExportScorecards` through its internal `ListComments` call to CSV output. Only criterion-linked comments are converted to plain text and appended to criterion-comment cells; all seven oversized AI-feedback records have no criterion and would be discarded. The observed failure therefore comes from unnecessary over-fetch, making a `CONVERSATION` type filter the first mitigation to validate before broader byte-aware handling.
- Clarified CONVI-7663's frontend trigger and request scope. The shared permission hook appeared across multiple surfaces, but Closed Conversations dominated the incident window with 749 failures; Performance Insights and Opera each had 12. An as-of read-only RCG query confirmed 3,330 distinct template resources across exactly 98 individual use cases, with one use case per resource and 294 repeated titles.
- Measured CONVI-7663's per-use-case concentration. `rcg-default` is largest at 126 templates (3.78%) and 2.79 MB stored JSON; the top ten contain 22.22%. A conversation-use-case filter would cut the observed worst-case request by 96.22%, while all-usecase surfaces still require a bounded permission contract.
- Assessed the quick use-case patch for regressions. It is safe as call-site alignment—permission lookup and the filtered list must receive the same effective use cases—but unsafe as a global hook-default change because notification/admin selectors intentionally span all use cases. Backend filtering retains global templates and supports overlap; RCG has no current or historical use-case reassignment for the affected resource set. Historical reassignment and multi/all-usecase modes remain required behavioral tests.
