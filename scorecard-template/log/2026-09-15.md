# 2026-09-15

- **Outcome:** Implemented CONVI-7671 in fresh `cresta-proto` and `go-servers` worktrees: backward-compatible image stripping for `ListComments`, nested conversation/type filtering for scorecard export, and opt-in use of the projection.
- **Validation:** Proto lint/build passed; full affected Go test suites passed against temporary locally generated bindings, including a 5 MiB inline-image response case.
- **Dependency:** Publish the proto contract first, then update go-servers to that version and rerun standalone validation. No local filesystem replacement remains in the intended go-servers diff.
- **PR:** Opened mergeable cresta-proto PR [#9876](https://github.com/cresta/cresta-proto/pull/9876) from `xw/convi-7671-list-comments-size`; CI was pending at handoff.
- **Dependent PR:** Rebased the backend change onto current `main`, pushed commit `40ec1dec37`, and opened mergeable draft go-servers PR [#32304](https://github.com/cresta/go-servers/pull/32304). Its interim compile dependency on the unreleased proto accessor is documented in the PR body.
- **Review verification:** Skipped a request to manually commit the stale `ListCommentsRequest` JSON schema. PR CI already generates and validates it while intentionally skipping the commit step; the post-merge `main` run owns the tracked generated commit, as confirmed by workflow conditions and schema history.
- **Published dependency:** Updated go-servers PR #32304 to cresta-proto v2.24.3, regenerated `go.sum`/`deps.bzl`, rebased onto current `main`, and force-pushed with a lease. Full collaboration and focused scorecard-export Bazel tests pass; the PR is mergeable with CI pending.
- **Review cleanup:** Inlined the single-use production `ListCommentsRequest` constructor and removed its dedicated unit test; retained a test-only expectation helper shared by 22 export cases. Focused export tests pass and commit `f32e79333a` is pushed.
- **Residual:** Byte-aware handling remains necessary for exceptionally large text and explicit single-comment-over-budget behavior.
