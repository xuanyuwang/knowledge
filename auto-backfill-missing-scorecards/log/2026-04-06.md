# Project Log - 2026-04-06

## Progress

- Investigated mismatch between RetrieveDirectorTaskStats (197) and RetrieveQAScoreStats (25) for brinks/care-voice
- Identified TWO root causes:
  1. **Data sync gap**: 116 of 197 scorecards missing from ClickHouse (all before March 30)
  2. **Criterion filter**: QAScoreStats filters by criterion `1b372658`, which only exists on a subset of scorecards

## Details

- PG historic has 197 scorecards, CH score_d has only 81
- Before March 30: ~88% missing from CH. After March 30: 0% missing
- The 16 pre-March-30 scorecards in CH were all re-updated after March 30, triggering a new CH write
- Something was fixed around March 29-30 that restored CH writes
- Even with perfect sync, the criterion filter would reduce 197 to ~56 (PG has 56 with that criterion)

## Cron Job: scorecard-sync-monitor

Updated the cron job on branch `xwang/scorecard-sync-monitor-task` in go-servers to:

1. **Run for all active profiles** (not hardcoded pairs) with optional customer filter via `SCORECARD_SYNC_MONITOR_CUSTOMER_IDS` envflag
2. **Accept template and usecase filters** via `SCORECARD_SYNC_MONITOR_TEMPLATE_IDS` and `SCORECARD_SYNC_MONITOR_USECASE_IDS` envflags
3. **Log structured reports first** — `[scorecard-sync-monitor]` prefixed lines with status (OK/WARNING/CRITICAL), total, missing, rate for each customer/profile
4. **Slack notifications as best-effort secondary** — builds Block Kit message with color-coded status emojis, filter context, and per-check stats
5. Two checks per customer/profile:
   - `checkHistoricSchemaMissing`: director.scorecards LEFT JOIN historic.scorecard_scores
   - `checkClickhouseMissing`: PG submitted IDs vs CH scorecard_d existence count

Build verified: compiles cleanly.
