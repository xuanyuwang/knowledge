# 2026-09-11

- Corrected CONVI-7642 staging read-back from the earlier explicit-line-based 83/176 count to an inheritance-aware 173/176 effective scopes across 28/29 customers. Forty-seven of 48 profiles are explicitly true and 126 child use cases inherit true; only `customerg/us-west-2` and its two use cases remain absent, with no false values.
- Confirmed production remains 1,963/1,963 effective scopes for the 360-customer safe cohort in `configv3`; Admin verification and the separately isolated `cresta-testing-ca` and The Zebra gaps remain.
- The Admin-generated Zebra PR #153777 passed its ConfigService dry run with exactly six effective `enableNAScore` additions, then merged as `4385ddaeebf975a3f36338c70f63c86942aa7f73`. Direct forward runs were cancelled by newer pushes, but succeeding non-dry-run run #34609424358 completed successfully and its artifact contains all six Zebra flag additions with no Zebra summarization change. Exact read-back remains before the requested production customer cohort reaches 1,969 effective scopes. `cresta-testing-ca` remains a separate internal test identity.
- Full production read-back run #34611200691 succeeded after the Zebra forward sync and auto-merged PR #153783, but neither contained a Zebra change. Current `configv3/prod/the-zebra` still lacks the profile flag, so Zebra is forward synced / read-back diverged and requires a targeted persistence/read-back repair before production customer completion.
- Scheduled read-back run #34621026458 later auto-merged PR #153802 with the Zebra profile `enableNAScore: true` value. The five child use cases contain no conflicting explicit values and inherit true, completing the requested production customer cohort at 1,969/1,969 effective scopes across 361 included identities. The rollout monitor was retired after recording completion on Linear.
- Added the final rollout summary to CONVI-7642 and moved the Linear issue from In Progress to Done. The comment records production completion, requested exclusions, the internal test identity boundary, and the non-blocking 173/176 staging read-back state.
- Addressed all review findings on coaching-qm-skills PR #6. Confirmed the preservation-entry contract consistently requires customer isolation and patch-level follow-up; changed the updater to explicitly reject inline `featureFlags` mappings rather than normalize unrelated YAML during a rollout; added two focused regression tests. Python compilation, unit tests, skill validation, diff checks, and CodeRabbit re-review pass. The only review thread is resolved and the stale bot changes-requested review was dismissed; the PR now awaits human approval.
