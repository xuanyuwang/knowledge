# 2026-06-29

- Refreshed this legacy knowledge project with `project.yaml`, `README.md`, and a session note under `sessions/2026-06-29/`.
- Investigated one-time backfill options for a scorecard template depending on Opera rule / LLM-evaluation outcomes.
- Found that scorecard-only backfill consumes existing annotations, while the downstream Opera Time Machine path can chain Opera annotation backfill and scorecard backfill.
- Initial recommendation: use or script a one-off `JOB_TYPE_DOWNSTREAM_BACKFILL_JOB` rather than a new recurring cron; only add a new flag override if a staging limited run proves the existing backend config path cannot evaluate the saved LLM/outcome policies.
- Followed up on customer-visibility risk for `enableLLMEvaluationMoment`: the flag appears to gate Opera authoring UI, not Closed Conversations annotation reads. Backfilled `LLM_EVALUATION` annotations can be loaded by the conversation annotation hook and should be considered visible unless hidden through Platform Visibility / `rolesWithVisibility` or an additional read-side/backend guard.
