# 2026-09-09

## RCG DTQ Performance Insights visibility investigation

- Investigated the #convo-intelligence ask ([thread](https://crestalabs.slack.com/archives/C04NB5AMV0F/p1788990592932079)): PI table for "Customer Xperience Center - Social Care DTQ - V1" empty for Andra Harders and other RCG users while Kath Cas sees data; QM Report shows scores.
- First live run of the new `investigate-analytics-data-issue` skill (coaching-qm-skills#3): mapped the template family in the app DB (69 same-titled instances; the data-bearing one is `019dbd42…` in cel-customer-xperience with 190 all-manual process scorecards across 12 agents), cross-checked ClickHouse (rows healthy, fresh, correct usecase), and resolved viewer roles in the auth DB.
- Initial ACL-scoping hypothesis (agent-only viewers scoped to themselves) was invalidated when Andra clarified she used her Cresta admin account: groundcover logs for her session show `isRootAccess=true` and `FinalUsers=10031` (unscoped).
- Groundcover forensics found the real signal: her RetrieveQAScoreStats queries ran 2–7s each (~20+ parallel, all-users population), one `context canceled` by the client plus an ES failure; RCG-wide 77 RetrieveQAScoreStats errors in 3h, all sampled `context canceled` — the frontend aborting slow requests during the same-day RCG PI latency incident (matches the parallel latency thread and ZenDesk #23745).
- Secondary candidate still open: same-titled template-instance confusion (three exact-title instances in cel-customer-xperience alone; new empty pair created 2026-09-03 in cel-co-outbound-sales). Template ids are placeholder params in query_log, so confirming which instance her browser selected needs the request payload or `user_history_correlation_id`.
- Also rejected with evidence: agent-only status forcing (flagd `enableScorecardPublish` off for rcg), sync gaps, usecase mismatch, "no template found" early return (zero warnings), FE cache.
- Secondary findings: process-scorecard `scorecard_time` = evaluated date (~2 weeks before submit), 8 CH rows with stale zero-epoch submit times.
- Open: confirm empty-vs-slow-loading and the exact template resource id with Andra; treat as part of the latency incident until then. Details: `sessions/2026-09-09/claude-rcg-dtq-pi-visibility.md`.
- **2026-09-10 update — root cause CONFIRMED via Andra's HAR capture** (20 `qaScoreStats:retrieve` entries): correct template `019dbd42`, correct usecase, no filters, `totalScorecardCount: 0` even for an August window that provably contains 51 in-window CH scorecards. Every request carried `conversationTimeRangeField: TARGET_FIELD_FOR_TIME_RANGE_CONVERSATION_ENDED_AT` — the default since director PR #22057 (CONVI-7586, merged 2026-08-26). The process-template filter strip never resets it; backend `needsConversationEndTime` then adds an INNER JOIN `scorecard.conversation_id = conversation.conversation_id`, eliminating all process scorecards (`conversation_id=''`). CH reproduction: 51 → 0. Per-user intermittency = persisted localStorage filter state predating the new default (Kath) vs fresh/default state (Andra, incognito included). Fix direction: FE omit/reset `dateRangeTarget` for process templates; BE skip the conversation join for process-only template sets. RCG tracking ticket: [CONVI-7674](https://linear.app/cresta/issue/CONVI-7674/rcg-process-scorecards-show-no-data-in-performance-insights-after) (High, In Progress).

## Analytics investigation skill published

- Distilled the Performance Insights / Leaderboard investigation methodology from this domain, scorecard-data-sync, and legacy ticket folders into the `investigate-analytics-data-issue` skill; opened coaching-qm-skills#3. Logged under the `ai-skills` project.
