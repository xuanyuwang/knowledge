# 2026-09-09

## CONVI-7600 lesson stats — second review follow-up + rebase (Claude Code)

Addressed [go-servers#32038 review comment r3961357366](https://github.com/cresta/go-servers/pull/32038#discussion_r3961357366) (reviewer: Hintful) on the in-progress-module-retake completeness semantic.

- Pinned down the behavior: a lesson assignment is complete only when every required module's latest attempt is scored. An in-progress/unscored retake on one module flips the lesson from complete(+passing) to incomplete (total-only). Verified this is consistent across all three stats surfaces — `RetrieveTrainingSimulatorLessonStats` (this PR), `RetrieveTrainingSimulatorTaskStats` (shipped: `buildAgentModuleStats`/`identifyCompletedAgents`), and `RetrieveTrainingSimulatorModuleStats` (shipped: `hasAttempt && scored` gate) — and matches the reporting spec ("complete only when every required module has an evaluated latest run"; latest attempt by `created_at` is the official outcome). Confirmed the scenario is real: `create_training_simulator_task_run` writes the `training_simulator_conversation_scores` row atomically with the run, with `agent_user_id` set and `score`/`passed` null until evaluation completes. Analysis doc: `.tmp/lesson-stats-incomplete-retake-analysis.md` (worktree). Decision: no completeness-rule change; add the requested regression test.
- Added `TestRetrieveTrainingSimulatorLessonStats_InProgressModuleRetakeFlipsPassingLessonToIncomplete` (agent passes both modules, then starts a newer unscored conversation retake on one module → assignment drops to `total_assignment_count` only). Reply posted as [discussion_r3968834887](https://github.com/cresta/go-servers/pull/32038#discussion_r3968834887).
- Rebased `claude/convi-7600-lesson-stats` onto `origin/main` to pick up cresta-proto v2.22.46 (stats count fields changed int64→int32, #32091). One conflict in `action_retrieve_training_simulator_module_stats.go` (convi-7638 lesson-scope parsing block vs our `parseStatsTimeRange` rename) — resolved by keeping both. Adapted lesson-stats aggregation and 25 test assertions from int64 to int32 (`492242ed1a`). Full `apiserver/internal/trainingsimulator` suite green (10.4s). Force-pushed.

Evidence: `work-items/lesson-module-statistics-reporting.md` changelog.

## CONVI-7656 — Testing RetrieveTrainingSimulatorModuleStats API (Claude Code)

Started black-box API testing on voice-staging (customer `cresta`, profile `walter-dev`).

- Confirmed the API is deployed: gRPC reflection on `grpc-cresta-api.voice-staging.internal.cresta.ai:443` lists `RetrieveTrainingSimulatorModuleStats` (and `...LessonStats`); handler is on `go-servers` `origin/main`.
- Bearer token obtainable via `cresta-cli cresta-token voice-staging cresta --bearer` (cached, ~1h, admin roles, VOICE scope).
- Diagnosed a gateway auth-layering quirk blocking direct gRPC: the fronting Go JWT gateway parses the raw `authorization` metadata without stripping `Bearer` (`Bearer <token>` ⇒ `tokenstring should not contain 'bearer '`; raw token ⇒ gateway forwards it without the `Bearer ` prefix the apiserver's `shared-go` interceptor requires ⇒ `No valid credential is provided.`). REST on the same host returns 415; the profile domain serves the SPA. Plan: `kubectl port-forward` to an apiserver pod + grpcurl direct with `Authorization: Bearer <token>`.
- Resolved the `walter-dev` app DB profile (cluster `voice-staging`, config `configv3/staging/cresta/walter-dev/config.yaml`).
- BLOCKED: `voice-staging` AWS SSO session expired — needed for both kubectl port-forward and the DB connection.

Plan + progress tracker: `sessions/2026-09-09/claude-convi-7656-module-stats-api-testing.md`. Work item: `work-items/CONVI-7656.md`.

### Resume and validation result (Codex)

- Restored AWS-backed read-only access without using the forbidden admin profile. `voice-staging_k8s_ro` and `voice-staging_k8s_contributor` can list pods but cannot port-forward; direct `grpcurl` now succeeds, so port-forward is unnecessary and the earlier gateway diagnosis is stale.
- Connected read-only to the `walter-dev` app DB through the approved skill and completed every feasible module-stats scenario. All 41 discover-all module identity/type/active-count tuples matched; targeted conversation, quiz, zero-assignment, dedupe, latest-only, lesson-scope, time-range, and never-started results matched DB expectations exactly.
- `RetrieveTrainingSimulatorLessonStats` is present in reflection, but every valid request returns `Unimplemented`; lesson happy-path testing is paused pending deployment of the CONVI-7600 handler.

### Lesson-stats deployment retry and initial validation (Codex)

- Confirmed the lesson handler is now deployed using a successful valid request rather than reflection alone.
- DB-backed tests passed for cross-profile/duplicate/inverted-range validation, all-30-ACTIVE-lesson discovery identity, a single-module aggregate, two multi-module completeness cases, an active zero-assignment lesson, and task-window time-range narrowing.
- Completed the remaining coverage: full discover-all aggregate digest, quiz-containing lessons, and latest-retake behavior all matched. An isolated task with 7 assignees and 9 historical scored attempts correctly returned zero applicable after the newest attempts became unscored.
- Replaced the Linear CONVI-7656 main description with the final results.
