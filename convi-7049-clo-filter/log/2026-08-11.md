# 2026-08-11

- Verified NCLH production CLO-MV flag readiness. NCLH runs in `us-east-1-prod`, which imports Insights `03-prod-main`; current GitOps declares `main-20260809_072100z-022144a6`.
- Confirmed go-servers routing commit `68c0fd7d` from [#30588](https://github.com/cresta/go-servers/pull/30588) is an ancestor of production image commit `022144a6`, so the declared release consumes `useConversationOutcomeMomentAnnotationMaterializedView`.
- Concluded config [#151310](https://github.com/cresta/config/pull/151310) is safe to merge with respect to release compatibility and the completed us-east CLO backfill. Live pod-image verification is still pending because the local AWS SSO session expired.
- Recovered the 2026-07-28 NCLH flag-off baseline: the representative CLO query took 85.830s and read 2.765B rows / 349.2 GB; its direct annotation component took 17.829s and read 2.017B rows / 245.4 GB. No equivalent Holiday Inn baseline exists, so capture one before enablement. Exclude legacy `holidayinn_chat` from blanket enablement because it has no trigger MV.
- Verified Holiday Inn config [#151311](https://github.com/cresta/config/pull/151311) excludes `holidayinn_chat`. Its additions cover only `club-voice`, `owners-voice`, `social-voice`, `transfers-voice`, and `voice`.
- Ran a low-volume NCLH production browser check with the user-confirmed CLO MV flag enabled. A 180-day daily conversion-only refresh cleared all visible loading regions in 37.5-40.5 s, directionally 52.8-56.3% lower latency (2.12-2.29x faster) than the 85.830 s historical raw-table CLO query and well under the 120 s frontend timeout. This is not an exact request pair or a p95 result.
- Session: `sessions/2026-08-11/cursor-nclh-prod-flag-readiness.md`.
- Browser performance session: `sessions/2026-08-11/codex-browser-mv-performance.md`.
