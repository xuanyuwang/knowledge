# Project Log - 2026-05-29

## Summary

- Backend PR is merged.
- Knowledge docs were refreshed to reflect the merged backend contract and the current frontend behavior.
- A detailed local FE test plan was added for manual and targeted automated validation.

## Active Contract

- Backend uses `submitted_scorecard_editors` / `submittedScorecardEditors`.
- The field shape is:
  - `users`
  - `teams`
  - `groups`
- Empty or unset submitted editors fall back to the existing edit-permission behavior.
- Runtime eligibility resolution is handled on the backend.

## Current Frontend Behavior

- Submitted-scorecard editors are configured in `TemplateBuilderAdvanced`.
- FE now supports users, teams, and groups in the selector.
- FE hydrates and saves all three buckets.
- The selector is not filtered by `permissions.scorecardGraders`.
- Changing `Who can use this scorecard` does not clear submitted editors.
- Empty-state text is `All users`.

## Documentation Changes

- Updated `README.md` and `project.yaml` to point at the active FE worktree and current objective.
- Rewrote `deliverables/plan.md` as a merged-backend contract summary.
- Rewrote `deliverables/fe-plan.md` to match the shipped FE behavior.
- Rewrote `deliverables/eng-design-doc.md` to reflect the merged contract and final FE behavior.
- Marked the 2026-05-22 audience pivot decision as superseded.
- Added a new final FE decision record for the 2026-05-29 behavior.
- Added `deliverables/local-test-plan.md`.
