# 2026-06-17 Log

## Summary

- `director#19805` was merged with the frontend client foundation for `EvaluateScorecardsPermissions`.
- The active FE runtime plan changed from purely reactive fail-and-freeze to proactive lock-on-load plus reactive fallback.
- The inline warning copy was finalized as:
  - `You do not have permission to edit this scorecard`

## API / FE Foundation

- New frontend client work is available through:
  - `CrestaAPI.coaching.evaluateScorecardsPermissions`
  - `useEvaluateScorecardPermissions`
- FE should request:
  - `SCORECARD_PERMISSION_MODIFY_SUBMITTED_LOCKED_SCORECARD`
- FE should only use this runtime permission for submitted scorecards in the submitted-lock scope.

## Active Runtime Behavior

- When a submitted scorecard loads, FE should query the runtime permission result.
- If denied, FE should lock the scorecard before any autosave/update operation can fire.
- If allowed, FE should keep the existing editable behavior.
- If the permission result becomes stale and `UpdateScorecard` returns 403, the existing rollback/freeze path remains the safety net.

## Next Implementation Work

- Merge latest `origin/main` into `director#19100`.
- Wire `useEvaluateScorecardPermissions` into conversation scorecard loading.
- Wire `useEvaluateScorecardPermissions` into process scorecard loading.
- Replace the previous fail-and-freeze warning copy with the finalized shorter copy.
