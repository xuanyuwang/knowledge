# 2026-09-08

## CONVI-7600 lesson stats — review follow-up: ACTIVE-only task scope (Claude Code)

Addressed [go-servers#32038 review comment r3961582621](https://github.com/cresta/go-servers/pull/32038#discussion_r3961582621) (reviewer: Hintful). The reviewer noted lesson stats selected ACTIVE+ARCHIVED DirectorTasks while module stats selected ACTIVE only.

- Decision: reverse lesson stats to ACTIVE-only so the two stats surfaces share one task-scope. The eng design / work item previously required ACTIVE+ARCHIVED "so archival does not erase history"; product intent for the v1 slice is in-flight assignments only, and divergent denominators between Lesson and Module tabs are worse than losing archived-task history now. Recorded in [decisions/2026-09-08-lesson-stats-active-tasks-only.md](../decisions/2026-09-08-lesson-stats-active-tasks-only.md); added superseded notes to the eng design and work item.
- Code (worktree `/Users/xuanyu.wang/repos/go-servers-milestone-2`, branch `claude/convi-7600-lesson-stats`): renamed `findLessonStatsDirectorTasks` → `findActiveLessonStatsDirectorTasks` and changed `task_status IN (ACTIVE, ARCHIVED)` → `task_status = ACTIVE`; doc comment updated to state DRAFT/DELETED/ARCHIVED exclusion and module-stats parity.
- Tests: `TestFindActiveLessonStatsDirectorTasks_StatusAndTimeWindow` drops `task-archived` from expected results; `TestRetrieveTrainingSimulatorLessonStats_EndToEnd` seeds `task-b` as ACTIVE so multi-task aggregation still validates without relying on archived-task inclusion.
- Focused lesson-stats suite green; full `apiserver/internal/trainingsimulator` package green (9.8s) against local PostgreSQL.

Evidence: `work-items/lesson-module-statistics-reporting.md` changelog.
