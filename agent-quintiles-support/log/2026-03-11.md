# Project Log - 2026-03-11

## Progress

- Implemented CONVI-6389: Aligned Coaching Plan `QuintileRankBadge` request with Coaching Hub defaults
- Created PR [#17263](https://github.com/cresta/director/pull/17263)
- Renamed `coaching-quintile-consistency-investigation.md` to `quintile-rank-behaviour-reference.md`
- Reorganized `quintile-rank-behaviour-reference.md` for mixed audience: behaviour summary (non-engineers) + collapsible code details (engineers)

## Details

### Changes in PR #17263

`QuintileRankBadge.tsx`:
- Removed `scorecardTemplateNames` prop — send `undefined` instead (= all templates, same as Coaching Hub default; BE treats both `undefined` and `[]` as "no filter")
- Added `scoreResource: QA_SCORE_RESOURCE_SCORECARD_SCORE`
- Added `includePeerUserStats: true`
- Added `criterionIdentifiers: []`
- Added `groupMembershipFilter: ALL_GROUP_MEMBERSHIPS`
- Simplified skip condition: just `!enableQuintileRank`

`AgentCoachingPlanHeader.tsx`: Removed `scorecardTemplateNames` from props interface and destructuring.

`AgentCoachingPlan.tsx`: Removed `scorecardTemplateNames` prop from `<AgentCoachingPlanHeader>` and the now-unused `scorecardTemplateNames` useMemo.

### Post-alignment consistency

Coaching Hub (default filters) and Coaching Plan now produce the same quintile rank. Both use: `undefined` templates, no user filter, last 7 days, `SCORECARD_SCORE`, `includePeerUserStats: true`, `criterionIdentifiers: []`.

Leaderboard and Performance (by criteria) also match when using default filters + last 7 days date range (both use `enableAutofailScoring: true` with default `SCORECARD_SCORE`).
