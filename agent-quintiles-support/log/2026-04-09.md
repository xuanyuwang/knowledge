# Project Log - 2026-04-09

## Progress

- Addressed PR review comments on [go-servers #26616](https://github.com/cresta/go-servers/pull/26616) (CONVI-6219: Wire directionality into UserOutcomeStats)

## Details

### PR #26616 Review Comments (sebastiancoteanu)

**Comment 1 — Separate `extractLowerIsBetterFieldDefs` from `filterFieldDefinitionsByFrequency`** (ACCEPTED)
- Reviewer suggested the lower-is-better extraction logic is a separate concern from frequency filtering
- Refactored: `filterFieldDefinitionsByFrequency` now returns `[]*metadatapb.FieldDefinition` instead of `([]string, set.Set[string])`
- New standalone function `extractLowerIsBetterFieldDefs(fieldDefinitions []*metadatapb.FieldDefinition) set.Set[string]`
- Caller in `RetrieveUserOutcomeStats` now does `fn.Map` + `extractLowerIsBetterFieldDefs` separately
- All tests pass

**Comment 2 — Make `FieldDefinitionNames` a `set.Set[string]` for consistency** (DECLINED)
- `FieldDefinitionNames` is iterated downstream in `buildOutcomeStatsMainWhereClause` to parse names and build SQL IN clauses — slice semantics fit better
- `LowerIsBetterFieldDefinitions` is used for membership checking (`.Has()`), which is why it's a set
- Different access patterns warrant different types; consistency isn't worth the conversion overhead
