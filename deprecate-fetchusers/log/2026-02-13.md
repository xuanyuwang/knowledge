# Project Log - 2026-02-13

## Progress

- Created project folder and Linear issue investigation
- Scoped work to `apiserver/internal/coaching` package only (8 call sites in 6 files)
- Created git worktree at `~/repos/go-servers-convi-6151` on branch `convi-6151-deprecated-the-usage-of-old-fetchuser-tool`

## Details

### Call Sites Summary
- **6 sites** need `map[string]*userpb.User` → use a new `FetchUserMapPaginated` helper
- **1 site** needs `includeUserMemberships=true` → use `CollectUsersPaginated` directly
- **2 sites** only need user IDs (map keys) → `FetchUsersPaginated` + extract IDs

### Approach
Add a `FetchUserMapPaginated` helper in `shared/user/utils.go` that wraps `CollectUsersPaginated` and returns `map[string]*userpb.User`. This minimizes the diff at each call site.
