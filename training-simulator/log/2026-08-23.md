# 2026-08-23

- **Review:** Extracted and visually verified all six comment threads from the downloaded Superhuman engineering-design PDF.
- **Filter scope:** Confirmed the existing page already supports agents, teams, virtual groups, and date range; reporting reuses those controls and omits `direct_team_only` unless Product expands scope.
- **Contract clarity:** Added complete protobuf examples for result, module-result, and criterion statistics; clarified active-lesson and legacy-warning semantics.
- **Deduplication:** Removed `current_passing_score` from statistics because the listed module's evaluation config already owns the current threshold.
- **Remote update:** Superhuman Docs URI support became available. Updated the live page, replied to all 12 active review threads, left them open for reviewer confirmation, and verified that stale Option 3 decision text was removed.
- **Statistics contract:** Consolidated lesson, module, and criterion calculations into one authoritative local-draft section, including previously implicit lesson retry/attempt and criterion rules. The remote page was not changed for this follow-up.
- **Reporting levels:** Reorganized the local statistics contract into Session, Lesson, Module, and module-criterion sections, with exact calculation units and formulas for each level.
- **Session API migration:** Specified additive `RetrieveTrainingSimulatorTaskStats` changes for zero-run tasks, assignment-rooted/direct DB reads, persisted result-state semantics, complete assignee rows, shared summary fields, and multiple-lesson handling. Clarified that legacy task-level `not_applicable` means the scalar score is unavailable, not necessarily that every result was genuinely N/A.
- **Session N/A compatibility:** Defined the existing per-agent field combinations for incomplete, completed all-N/A, passed, and failed. Director must check `not_applicable` before mapping `COMPLETE && !passed` to failed.
- **Session scope refinement:** Removed repeated session metrics from the local draft. Kept only missing incomplete-assignee and per-agent side-panel requirements; reduced the proposed response addition to per-agent `attempt_count` plus the independently required legacy-data warning.
- **Figma-strict lesson/module scope:** Removed non-UI assignment, adoption, N/A, attempt, and retry fields from lesson/module reporting. Kept only Sessions assigned, Active Lessons, average/pass values, module pass chips, and module/criterion pass fractions shown in Figma.
- **Remote sync blocked:** Tried to replace the existing Superhuman page body with the local draft while preserving comments. The document connector was unauthorized and no app/browser control connection was available; no remote content or comments changed.
- **Evidence:** `sessions/2026-08-23/codex-superhuman-comment-review.md`.
