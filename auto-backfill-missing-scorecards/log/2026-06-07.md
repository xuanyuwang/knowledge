# 2026-06-07

## Rollout plan for CONVI-6869

Added `rollout-plan.md` with a phased staging and production rollout plan for the scorecard auto-heal cron:

- rollout controls before scheduled runs
- code deploy with auto-heal disabled
- staging dry-run validation
- constrained staging auto-heal
- production observe-only
- production canary
- gradual production expansion
- config-first rollback

Verified the checked-in local state and concluded we should **not** start from Step 3 yet:

- staging `cron-scorecard-sync-monitor` is still suspended and daily
- the monitor calls backfill directly when missing scorecards are detected
- no dry-run / `SCORECARD_SYNC_MONITOR_ENABLE_AUTO_HEAL` gate exists yet
- no max-heal cap or auto-heal allowlist exists yet

Minimum prerequisite before Step 3: add and deploy the auto-heal gate with default `false`, plus a cap and allowlist, then unsuspend staging with auto-heal disabled.
