# 2026-08-13

- Reviewed Cursor review 4928927631 and CodeRabbit review 4928931548 on go-servers PR 31048 against branch commit `bd1b6d6e72`.
- Validated two correctness findings (wildcard filters and overview revision identity), two minor quality findings (error context and protobuf getters), and one reliability-policy concern (fail-open versus fail-closed enrichment).
- Cross-checked the deeper implementation history: propagation of template-query errors was an explicit earlier PR-review change, and the frontend now assumes backend-provided `criterionDisplayName`. Fail-closed behavior therefore matches the recorded contract unless availability is deliberately reprioritized.
- Recorded detailed examples and recommended fix order in `sessions/2026-08-13/codex-pr-31048-review-findings.md`.
- Traced Director `ListCoachingPlans` consumers at FE commit `d2ff182d6b`: focus-filtered Coaching Hub calls do not consume criterion display names, but the active agent coaching-plan details path also uses List and directly depends on them for plan criteria/deactivated-criteria rendering. Recorded an explicit projection/view as the safe optimization boundary.
- Designed a storage-compatible SQL fix for wildcard focus filters: retain exact array overlap for legacy/concrete IDs and add an OR `EXISTS` over unnested IDs normalized from `template@revision/criterion` to `template/criterion`; confirmed no existing GIN index on the focus-criteria array.
- Enumerated production ListCoachingPlans focus-filter callers: CriteriaGoalSnapshot forwards target `@*` names, while AddGoalPopover passes the concrete current template name. Both are target-based flows; the latter may be accidentally revision-specific and miss older-revision plan assignments. No internal go-servers production caller supplies this filter.
- Evaluated a strict filter contract: target-originated `T@*/C` matches legacy and all revisions; plan-originated `T@R/C` matches only that explicit revision. The model is sound for new data, with an explicit legacy caveat because legacy `T/C` rows are returned with inferred concrete revisions but cannot be strictly matched back without migration/provenance.
- Rechecked whether CONVI-7379 can be session-only: ListCoachingSessions enrichment fixes collapsed session labels, but the session editor derives options from ListCoachingPlans plan criteria and the paired FE relies on plan `criterionDisplayName` for deactivated items. A session-only design requires FE option merging plus consistent enriched/refetched write responses, or a List-then-Get plan flow.
- Evaluated a minimal read-only alternative: keep `template/criterion` storage, resolve the latest template revision containing each criterion to populate only `criterionDisplayName`, and keep response revisions wildcard because lookup revision is not historical provenance. This resolves the Pure stale-ID case and removes the revision-format/filter/overview complexity, assuming criterion IDs are stable logical identities across revisions.
- Scoped the matching FE change: remove current-template criterion-name reconstruction from plan/session response renderers and consume `criterionDisplayName` directly, while retaining current-template metadata maps needed for criterion classification, ALO/effectiveness behavior, and template labels. Identified `CoachingPlanHistoryModal` as an additional ListCoachingPlans consumer that should prefer the backend name.
- Moved the existing BE branch into `/Users/xuanyu.wang/repos/go-servers-convi-7379` and anchored FE commit `d2ff182d6b` on new worktree/branch `/Users/xuanyu.wang/repos/director-convi-7379` / `xw/convi-7379-focus-criteria-display-names-fe`.
- Reworked the BE working tree to preserve `templateID/criterionID` writes, resolve labels from the newest revision containing each criterion, retain wildcard response revisions, and remove revision-aware filter behavior. Added resolver/write-format regression coverage; focused plus endpoint-level coaching tests passed.
- Updated FE history rendering to consume backend `criterionDisplayName` and remove its current-template name map. The paired plan/session UI tests (17) and changed-file ESLint passed.
- Formalized the decision change: after encountering mixed-format backward compatibility and especially incompatibility with revision-independent targets, rejected revision-qualified writes and selected read-path-only display-name recovery. Added a decision record, canonical CONVI-7379 work item, README rationale, and a superseded notice on the 2026-08-12 implementation record.
- Squashed/replaced the BE branch history onto its original merge base, leaving one read-path-only commit (`6ebc7a750f`). Verified the net diff is limited to three read handlers, transformer enrichment, and tests; mutation/write/filter/target/overview files match the base. Re-ran the affected coaching suites successfully and force-pushed with an exact lease.
- Updated go-servers PR #31048's description to remove the superseded revision-qualified write design and document the final `templateID/criterionID` persistence, newest-containing-revision label lookup, target compatibility rationale, FE dependency, and test coverage.
