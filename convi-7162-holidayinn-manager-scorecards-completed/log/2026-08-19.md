# 2026-08-19

- Created config PR https://github.com/cresta/config/pull/151886 to enable `filterByScorecardSubmitTime` globally.
- Updated 1,861 production profile/use-case feature-flag maps across 354 customer history files, including all 13 Holiday Inn maps.
- Excluded 17 Schwab/Comcast-family config files (117 maps) because those customers use separate release flows.
- ConfigService schema validation, targeted YAML validation, completeness/order checks, and `git diff --check` passed.
- Full repository CI reached an unrelated existing staging config validation failure outside the PR diff.
- Merged latest `origin/master` and resolved one simple conflict by preserving master's deletion of the obsolete `cresta-compass-agent` profile; added the flag to 11 newly introduced eligible maps.
- PR is mergeable again; required checks are queued.
- After the global flag was enabled, reproduced the exact Cliff Hawker Aug 17 production scenario on the official Holiday Inn `/director/` route.
- Original Aug 14–18 range now shows Cliff as `2, N/A, N/A, 2, 2` (6 total); the previously incorrect Aug 17 cell is now 2.
- Narrowing the range to Aug 17 returns 2 in the Leaderboard and `2 scorecards across 1 template` in the Ride Along Template breakdown.
- Verification result: PASS; the exact reported mismatch (Leaderboard 1 vs details 2) no longer reproduces.
