# Project Log - 2026-03-10

## Progress

- Noted GROUP branch FinalUsers mismatch as a known edge case (`group-branch-finalusers-mismatch.md`)

## Details

Flagged by CodeRabbit on ext table PR #26178. When both user filter and group filter are set, `FinalUsers` (used for CH WHERE clause) can be wider than `UserNameToGroupNamesMap` (used for group bucketing), causing top-level totals to include unbucketed user data. Affects conversation_stats, suggestion_stats, summarization_stats. Pre-existing behavior — needs verification whether it's intended.
