# 2026-05-27

- Reviewed Groundcover results for `POST /cresta.v1.coaching.CoachingService/UpdateScorecard`.
- Confirmed the `@UpdateScorecard Error Rate` dashboard is filtered to `workload:apiserver`, so it will not show errors from `cpu-2xlarge-67vrl-runner-*` workloads.
- Groundcover re-query still showed `UpdateScorecard` errors, but the observed spans were from runner workloads rather than `apiserver`.
- The runner errors split into two known backend outcomes:
  - gRPC `code=5 NOT_FOUND`, which matches the fresh-scorecard lookup path in `UpdateScorecard`
  - gRPC `code=7 PERMISSION_DENIED`, which matches the explicit permission gate in `UpdateScorecard`
- For CONVI-6841, the customer-facing incident signal remains `apiserver` `UpdateScorecard` `NOT_FOUND` errors during create-then-autosave. Runner-only errors should be treated as test/automation noise unless a matching `apiserver` pattern appears.
