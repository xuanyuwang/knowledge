# 2026-09-09

## Authenticated gRPC endpoint testing skill

- Added `test-grpc-endpoint` on branch `xw/test-grpc-endpoint-skill` in `coaching-qm-skills`.
- Added a reusable caller that obtains a short-lived bearer token through `cresta-cli`, rejects caller-supplied authorization metadata, keeps tracing disabled, and supports TLS or explicit local plaintext endpoints.
- Captured the DB-backed black-box workflow: validate the real RPC rather than reflection alone, derive expectations independently, normalize protobuf JSON representation, and record per-scenario evidence without secrets.
- Validation passed: shell syntax, negative argument/auth-header checks, the skill package validator, and a live read-only `RetrieveTrainingSimulatorLessonStats` call on voice-staging.
- Rebased onto current `origin/main`, committed as `085fa85`, pushed branch `xw/test-grpc-endpoint-skill`, and opened [coaching-qm-skills#2](https://github.com/cresta/coaching-qm-skills/pull/2).
- Created [CONVI-7659](https://linear.app/cresta/issue/CONVI-7659/add-reusable-grpc-endpoint-testing-skill) for the PR and updated the PR title to use the new issue ID. The issue is team-scoped to CONVI with no unrelated project assignment.
- Addressed both PR security comments in `cef6f53`: plaintext now resolves and rewrites only loopback endpoints before auth, and an exact reviewed method allowlist rejects unapproved/mutating RPCs before token acquisition. Revalidated the package, live TLS call, loopback plaintext path, and both rejection paths; replied to and resolved both threads. CodeRabbit re-review remains pending while all other checks pass.

## Analytics data-issue investigation skill

- PR #2 merged as `055310f`; cut branch `xw/investigate-analytics-data-issue-skill` from updated `origin/main`.
- Synthesized the Performance Insights / Leaderboard data-issue investigation methodology from the analytics domain, scorecard-data-sync, and the legacy ticket folders (see session note for the full source list).
- Added `investigate-analytics-data-issue` to `coaching-qm-skills`: workflow + recurring pitfalls in `SKILL.md`, plus references for metric→API→table mapping, PG source-of-truth queries, ClickHouse cross-check queries, and a root-cause catalog with prior cases and rejected hypotheses. Connections delegate to the existing connect-customer-* skills; all access read-only.
- Committed as `fe7efac` and opened [coaching-qm-skills#3](https://github.com/cresta/coaching-qm-skills/pull/3).
