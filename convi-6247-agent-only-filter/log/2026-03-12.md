# Project Log - 2026-03-12

## Progress

- Rebased PR #16777 on latest `origin/main` — one conflict in `localFeatureFlags.ts` (both main and our branch added entries at end of union type).
- All CI checks passed: Lint, Typecheck, Test, Build, E2E, CodeRabbit, security scans.
- **PR #16777 merged** (squash merge).

## Details

### Conflict resolved

`localFeatureFlags.ts`: main added `enableKBCrestaAdminTools`, our branch added `enableAgentOnlyFilter` — both at the end of `LocalFeatureFlag` type. Kept both.

### Feature flag

The feature flag commit (added by user before this session) guards the "Agents only" filter behind `enableAgentOnlyFilter` on Performance page.

### Phase 4: Leaderboard filter implemented

New branch `xuanyu/convi-6247-leaderboard-filter`, PR [director #17314](https://github.com/cresta/director/pull/17314).

Changes to `useLeaderboardsFilters.tsx`:
- `listAgentOnly` added to `DefaultLeaderboardsFiltersState` Pick type, `LocalStorageLeaderboardsFiltersState`, `INITIAL_LEADERBOARDS_FILTERS_STATE` (default `true`), `localStateToFilterState` (`?? true` fallback), `filterStateToLocalState`
- BooleanFilter component with `isDisabled` on Manager tab
- `getFilterMenuOptions()`: added agents-only entry with i18n
- `FILTER_STATE_ACCESSORS`: added `listAgentOnly`
- `FILTERS_SELECTION_HOOKS`: added `useListAgentOnlyLevelSelect`
- Feature flag: `enableAgentOnlyFilter` hides filter and strips `listAgentOnly` from state
- `modifiedFiltersState` memo replaces direct `modifyFilterStateForNoTemplateValue(filters)` to support flag guard

Locale: added `leaderboards.filters.agents-only` key.

## Next Steps

- Phase 5: Leaderboard API pass-through (replace hardcoded `filterToAgentsOnly: true` on Agent/Team tabs)
- Phase 6: Agent Assist (TBD)
