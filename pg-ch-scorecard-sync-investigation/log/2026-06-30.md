# 2026-06-30

- Investigated `cron-scorecard-sync-monitor` 30-minute deadline failures after PR `#29273` / `ef07ac421a` changed unsubmitted scorecard inventory to exclude shells with no `director.scores`.
- Verified current `origin/main` inventory query combines submitted and unsubmitted scorecards with an OR; unsubmitted branch uses `created_at` plus correlated `EXISTS` against `director.scores`.
- Found the same PR added `countExcludedUnsubmittedWithoutScores`, a second unsubmitted-shell query with `NOT EXISTS`.
- Live `oportun/us-west-2` `EXPLAIN` showed the current inventory query has a high-cost unsubmitted scorecards branch and the excluded-count query plans a parallel seq scan over `director.scorecards`; bounded `EXPLAIN ANALYZE` for the excluded count timed out at 45s.
- GroundCover showed `oportun/us-west-2` logged `START` at 10:00:58Z and no completion before the pod was deleted at 10:30:00Z; `cvs/us-west-2` completed ClickHouse stats and `END` at 10:29:28Z, making PG inventory/count the best-supported bottleneck.
- Proposed minimal fix: split submitted/unsubmitted inventory, keep submitted on `submitted_at` index, remove or gate the excluded count from the critical path, add timing logs, and consider a concurrent partial `(customer, profile, created_at)` index for unsubmitted standard non-calibrated scorecards if needed.
- Populated Linear ticket `CONVI-7186` with the longer-term prevention path: avoid creating empty AutoQA scorecard shells when AutoQA maps/computes no usable scores. The ticket documents why a supporting index would help but is expensive, and scopes implementation to live AutoQA plus AutoQA backfill guards.
