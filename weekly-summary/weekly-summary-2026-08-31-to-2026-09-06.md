# Weekly Summary — 2026-08-31 to 2026-09-06

**Created:** 2026-09-08
**Format:** PPP (Progress / Problems / Plan), matching the `#ppp-engineering` thread style.
**Evidence gap:** Linear was not queried for this wrap; statuses below come from the durable daily logs and PR merge records in this repo.

## Progress

- **Training Simulator session-stats backend (CONVI-7583):** Shipped Milestone 1 end to end — [go-servers#31780](https://github.com/cresta/go-servers/pull/31780) and the contract PR [cresta-proto#9716](https://github.com/cresta/cresta-proto/pull/9716) are merged. Backend now does assignment-rooted zero-run reporting, deterministic latest-attempt selection, bounded task-run reads with a 100k overflow probe, and per-agent `attempt_count` — the only new response datum, confirmed by an independent contract audit.
  - Verified and fixed the last review finding before merge: a filtered zero-run task could fall back to the wrong configured lesson; added regression coverage for the two-lesson case.
  - Resolved the audience review thread with a deliberate semantic boundary: request users/groups select tasks, while metrics and `AgentPerformance` use each matched task's full stored audience.
- **Training Simulator Director frontend (CONVI-7584):** Implemented the focused FE deltas on top of the merged backend — filtered unique-agent incomplete count and authoritative per-agent drawer attempt counts. 40 focused tests, TypeScript, lint, and hooks pass; opened draft [director#22413](https://github.com/cresta/director/pull/22413) with zero CodeRabbit findings.
- **Module-stats backend (CONVI-7601):** Reviewed the draft (verdict: request changes — conversation score divided by 100, archived lessons counted in `active_lesson_count`, three contract violations), then drove it to merge: implemented quiz `question_stats` + `module_type` per [cresta-proto#9769](https://github.com/cresta/cresta-proto/pull/9769), fixed configured-zero passing score (explicit-0 now means "every scored attempt passes"), and landed it all via [go-servers#31952](https://github.com/cresta/go-servers/pull/31952) (merge commit `25d3566458`).
- **Lesson-stats backend (CONVI-7600):** Replanned into two review gates after the CONVI-7601 findings, implemented both gates (parse/validate, lesson-revision query pattern, batched latest-attempt loader, aggregation + RPC wiring), adapted to the merged #9769 contract (empty `training_lesson_names` = all ACTIVE lessons, ordering guarantee dropped), and opened [go-servers#32038](https://github.com/cresta/go-servers/pull/32038) with the configured-zero passing-score parity fix.
- **Reporting proto contracts:** Merged [cresta-proto#9677](https://github.com/cresta/cresta-proto/pull/9677) (lesson-stats contract, released as v2.22.7) and #9769 (shared `TrainingModuleType`, quiz question breakdown, REQUIRED→OPTIONAL field behavior), including a field-number compatibility correction so the shipped module `time_range = 3` is preserved.
- **Submitted-scorecard lock (CONVI-7612):** Reviewed [director#22401](https://github.com/cresta/director/pull/22401) and found a merge-blocking state split — `lockReason` disables fields but autosave/submit guards still use the narrower legacy `readOnly`; also flagged the Cresta-admin authorization dependency on [go-servers#31889](https://github.com/cresta/go-servers/pull/31889) and the missing lock-messaging parity follow-up.
- **Workspace hygiene:** Reconciled local worktrees with Linear status — removed 17 clean worktrees for 11 completed tickets, pruned stale registrations, updated `workspace/repos.yaml`.

## Problems

- **CI infra flakes:** The first #31952 run failed only on `apt` hash-sum / tzdata mirror flakes; re-ran the failed jobs and the rerun went green with auto-merge firing. No code errors.
- **Review automation stalls:** `/code-review` stalled twice on stream-watchdog infra flakes (one leaving a broken partial edit that had to be completed manually). Proceeded on documented manual review both times.
- **Local test environment:** Embedded-PostgreSQL shared-memory exhaustion blocked DB tests once; recovered with the documented cleanup, created the missing local `cresta` role, and full suites ran green afterward.
- **Contract drift found:** Shared `TimestampRange` documents `[start, end)` while task SQL uses `<= end`, and the existing task-stats proto wording doesn't match shipped behavior — documented, with the pushed contract wording matching shipped backend behavior.
- **Open gates for Milestone 1 release:** Partial-timeout classification, reporting row-scope authorization, and staging/design reconciliation remain undecided; the merged proto comment still incorrectly limits attempt count to required modules.

## Plan

- Land CONVI-7600: shepherd [go-servers#32038](https://github.com/cresta/go-servers/pull/32038) (lesson-stats backend) through review to merge.
- Land CONVI-7584: take [director#22413](https://github.com/cresta/director/pull/22413) out of draft, decide on the inherited loading-badge / stats-error fallback behavior, and merge; correct the stale `attempt_count` proto comment.
- Close out CONVI-7638: lesson-scoped module stats ([go-servers#32039](https://github.com/cresta/go-servers/pull/32039), opened Mon 09-07) through review.
- Drive Milestone 1 release gates: partial-timeout classification and reporting row-scope contracts, plus staging/design reconciliation.
- Follow up on CONVI-7612 review findings for director#22401 once go-servers#31889 lands.
