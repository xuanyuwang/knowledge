# 2026-07-18

## Time Range Filter Investigation

- Completed full investigation of time range filter → timestamp column mapping for Performance Insights and Leaderboard pages.
- Traced all 12 backend analytics APIs to their exact timestamp columns and database tables.
- Refined to column-level granularity: each column in a leaderboard table row can filter on a different timestamp.
- Key findings:
  - PI Conversations tab: all `scorecard_time` (consistent)
  - PI Agent Outcomes tab: all `outcome_time` (consistent)
  - Agent/Team Leaderboard: mixes `conversation_start_time` (most columns) with `scorecard_time` (QA/scorecard columns)
  - Manager Leaderboard: 5 APIs, 4 different timestamp columns (`event_time`, `scorecard_time`, `scorecard_submit_time`, `manager_submitted_at`), spanning ClickHouse and PostgreSQL
- RetrieveCoachingSessionStats is the only analytics API hitting PostgreSQL (`manager_submitted_at` on `director.coaching_sessions`)
- Deliverable: `deliverables/time-range-timestamp-mapping.md`
- Remaining: process scorecard `scorecard_time` population, `conversationTimeRangeField` usage check
