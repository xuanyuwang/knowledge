# 2026-09-18

- Validated two review comments on Director PR #22809 without changing product code.
- The permission-hook options-object suggestion is valid but requires a 13-production-caller migration across surfaces intentionally outside CONVI-7663; recommended a focused follow-up rather than a mixed API.
- The configurable waiting flag can be removed, but not by always skipping when `Conversation.usecase` is absent because that field is optional and other coaching-detail consumers exist.
- Recommended deriving permission skipping from coaching-details enablement and base-conversation loading state, then preserving the unscoped fallback for a loaded conversation with no use case.
