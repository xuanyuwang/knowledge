# Project Log - 2026-03-17

## Progress

- Investigated wrong user-to-team mapping for bswift customer
- Root cause: duplicate GUEST_USER accounts passing ListUsersForAnalytics filters
- Cross-checked app.chats: all conversations route to GUEST user_ids, PROD user_ids have zero convos
- Scope: only 3 affected agents, all in bswift
- Documented findings in `wrong-team-mapping-investigation.md`

## Details

Agent "Briana Joyner" displayed under team "default" on Performance page. Found two user accounts with same name — active PROD_USER in Nathaniel King team, and old inactive GUEST_USER (type=2) in "default" group.

### Why GUEST_USER passes filters
- `ListUsersForAnalytics` only filters `type != DEV_USER(3)`, not GUEST_USER(2)
- With `IncludeInactiveUsers=true`, inactive profile is included
- GUEST has `user_roles = {AGENT}`, passing AgentOnly filter

### Critical finding: platform routing
All 3 duplicate agents have conversations created under GUEST_USER ids:
- Briana Joyner: 0 PROD convos, 1752 GUEST convos (still active)
- Brenda O'Neal: 0 PROD convos, 575 GUEST convos (still active)
- Tennisha Cox: 6 PROD convos, 418 GUEST convos (still active)

This means ClickHouse cleanup alone won't fix it — platform integration is still routing to old GUEST accounts.
