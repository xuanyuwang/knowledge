# 2026-07-08

- Implemented CONVI-7206 fix: gate apiserver submitted-scorecard editor enforcement behind per-customer `disableEditingOnSubmittedScorecards` Director config flag (config service, not flagd).
- Corrected approach after review: flux-deployments#301532 is wrong mechanism; go-servers#29774 reads `legacy_public_config.featureFlags` via `configServiceClient.GetProfileConfig`.
- Moved CONVI-7206 backend work to worktree `/Users/xuanyu.wang/repos/go-servers-convi-7206` on branch `convi-7206-gate-submitted-scorecard-lock-behind-ff`.
- Babysat go-servers#29774: fixed CodeRabbit feedback, restored one-commit PR, CodeRabbit approved and automated checks passed except long-running `Run apiserver coverage (coaching)` still pending; merge remains blocked by codeowner approval.
- Fixed failed `Run apiserver coverage (coaching)` job for go-servers#29774: `TestResetScorecard` needed a profile-config mock for the evaluator's internal flag lookup; amended one-commit PR to `e96201964e` and force-pushed, triggering fresh CI.
- Investigated fresh `Run apiserver coverage (heavy-a)` failure in run `28968988165`: failure is unrelated to CONVI-7206, in `//apiserver/internal/internaljob:internaljob_test` shard 1 from an unexpected `CreateNotification` mock call during `TestStartChunkedProcessing...`; requested rerun of failed jobs after the original workflow completed.
- Fixed repeated `Run apiserver coverage (coaching)` timeout in run `28968988165`: `TestUpdateScorecard` also needed the profile-config mock for evaluator-internal flag lookup; verified focused coaching tests locally, amended one-commit PR to `140f1d8399`, and force-pushed fresh CI.
