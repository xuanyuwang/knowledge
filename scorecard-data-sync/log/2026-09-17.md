# 2026-09-17

- Opened the RCG `Consumer Outreach - Casino DTQ` 50%-versus-80% investigation for conversation `01a08bec-b88b-7768-809b-e7b2757f710c`.
- Used Glean first to confirm the root Slack report and indexed replies through 19:34 UTC; its index had not yet captured the later race hypothesis.
- Resolved production scope to `rcg/us-east-1`, AWS account/cluster `us-east-1-prod`, database `rcg-us-east-1`; production querying completed after the user refreshed AWS SSO.
- Traced current score persistence: a partial request computes the parent total from only incoming scores, while `mergeScoresData` retains persisted criterion rows missing from the request. Out-of-order partial autosaves can therefore leave PostgreSQL criteria implying 80% while `scorecards.score` remains 50%.
- Confirmed exact production state for scorecard `01a09154-585f-71cf-8809-c95bf13e165e` on active template `019d521b-68c7-727e-bead-301e50d92c7c@c1af7a35`: five equally weighted criteria are `100/0/100/100/100`, yielding 80%, but the parent score is 50%.
- Stopped before ClickHouse because PostgreSQL is wrong. No PostgreSQL/ClickHouse data was mutated and no reindex/backfill was triggered.
- Verified repair options: Temporal scorecard reindex only rebuilds ClickHouse and cannot fix this source row. The suspended `cron-manual-scorecard-scores-backfill` can recompute PostgreSQL from the persisted full score set via `GetScorecard` -> `UpdateScorecard`, but RCG's current config uses an obsolete August 2025 range and the common deployment has the inner task disabled. A narrowly scoped one-shot cron run is preferable to direct SQL.
- Reviewed go-servers PR #32447. It likely fixes the incident's leading submit-overwrites-newer-autosave path: submission now omits scoring fields and reloads committed state, and the production submission/update timestamps are only 516 microseconds apart. It remains a mitigation because concurrent `UpdateScorecard` calls can still commit stale snapshots out of edit order without a revision/precondition or transaction-final aggregate recomputation.
