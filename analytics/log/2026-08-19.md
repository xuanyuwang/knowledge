# 2026-08-19

## CONVI-7378 follow-up

- Investigated Jimmy Skelton's newer SCAN conversation and corrected the prior old-revision-only explanation.
- Production PostgreSQL and ClickHouse agree: the manual Consent to Call override persisted, but scorecard revision `7290d017` maps Yes to zero points.
- Confirmed the July correction was reverted in the August 4 revision and remains inverted in the latest observed August 17 revision.
- Drafted a customer response explaining the 83.3% section / 95.8% overall math, the active template correction needed, and the existing backfill limitation for manually edited scorecards.
- Evidence: `sessions/2026-08-19/codex-convi-7378-follow-up.md`; current state: `work-items/CONVI-7378.md`.

## CONVI-7402 QA metadata filter OR semantics (Team value + (no value))

- Implemented ClickHouse QA overlap handling for a MomentGroup that contains the same metadata moment template id in both `moments` and `excluded_moments` (concrete values + `(no value)`).
- Updated both QA ClickHouse query builders (score stats + conversations) to express overlap as `LEFT JOIN` + `WHERE (valueExists OR missingBranchIsTrue)`, while preserving existing include-only/exclude-only behavior and AND-ing unrelated exclusions.
- Added parser/unit coverage plus SQL golden regressions:
  - `testdata/clickhouse_RetrieveQAScoreStats_FilterByMetadataMomentGroups_OverlapValuePlusNoValue_request.sql`
  - `testdata/clickhouse_RetrieveQaConversations_FilterByMetadataMomentGroups_OverlapValuePlusNoValue_request.sql`
- Validation: `gofmt` on touched Go files, focused `go test ./insights-server/internal/analyticsimpl` overlap tests passed, and `bazel run //:gazelle` is up-to-date.
