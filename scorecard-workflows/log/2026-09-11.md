# 2026-09-11

- Started CONVI-7682 in `/Users/xuanyu.wang/repos/director-convi-7682` on `xw/convi-7682-consistency-score-percentage` from current `origin/main`.
- Completed the CONVI-7682 fix: the Group Calibration progress table now normalizes its 0-100 consistency score before locale-aware percentage formatting. Director app lint, full TypeScript checking, the shared formatter suite (18 tests), and diff validation passed. See `work-items/CONVI-7682.md` and `sessions/2026-09-11/codex-convi-7682-consistency-percentage.md`.
- Verified the CONVI-7682 data contract against Woolworths production and backend code: the DB consistency column is null throughout; `ListScorecards` dynamically computes a 0-100 value using `100 * matches / criteria`. This confirms the defect and fix are at the frontend formatting boundary.
- Opened draft Director PR #22763 from commit `4b301086af` using the repository template. Functional smoke steps are complete in the description; full-screen proof and the preview environment link remain pending.
- Confirmed the `9000%` defect is a regression from Director PR #14756 (`d3d29a030ba`, merged 2025-11-07): an i18n migration replaced a 0-100 formatter with a 0-1 formatter without input normalization or display-level test coverage.
- Patched PR #22763's current remote description with the regression evidence while preserving subsequent remote QA edits, including both proof videos and the staging environment link.
