# Project Log - 2026-05-27

## Decisions Captured

- The active backend execution contract remains the one in `deliverables/plan.md`, not the older audience-pivot draft.
- For FE, submitted-editor configuration in Director will be implemented as a user-only picker in the template builder.
- The FE picker will depend on the existing "Who can use this scorecard" permission (`permissions.scorecardGraders`).
- The FE picker will filter visible users by the selected grader roles.
- The FE picker will not expose teams or groups in this slice.
- If grader-role selection changes, FE will clear all currently selected submitted-editor values.
- Backend may still validate a broader `users + teams + groups + grader-permission` rule set; FE does not need to expose the full backend surface right now.
- The stale Director draft PR for this feature should be closed and replaced with a fresh worktree/branch based on current `origin/main`.

## Repo State Observed

- Existing Director worktree:
  - `/Users/xuanyu.wang/repos/director-convi-6862`
  - branch: `xwang/convi-6862-submitted-scorecard-editors`
  - open draft PR: `#18928`
- Existing go-servers worktree:
  - `/Users/xuanyu.wang/repos/go-servers-convi-6862`
  - branch: `convi-6862-submitted-scorecard-editors`
  - open PR: `#28171`
- Director main checkout in `/Users/xuanyu.wang/repos/director` is dirty from unrelated local changes and should not be reused directly for feature work.

## FE Execution Plan

1. Close stale Director PR `#18928`.
2. Remove the old `director-convi-6862` worktree.
3. Create a fresh `director-convi-6862` worktree from `origin/main`.
4. Implement the template-builder submitted-editor field in that fresh worktree.
5. Validate with focused FE tests.

## Notes

- The go-servers PR/worktree was left untouched during FE planning because it may still be active backend work rather than stale FE work.
- If backend contract details change again, `deliverables/fe-plan.md` should be updated before further FE iteration.

## FE Implementation Applied

- Implemented the FE work in `/Users/xuanyu.wang/repos/director-convi-6862` on branch `xwang/convi-6862-submitted-scorecard-editors-v2`.
- Added `permissions.submittedScorecardEditors` to Director's local scorecard-template model as a `users/teams/groups` object.
- Added transformer fallback logic so Director can read the field even though the generated web-client type on `origin/main` does not yet expose it.
- Added template-builder form state for submitted editors using `UserTeamGroupSelection`.
- Hydration intentionally keeps only `users`; FE continues to skip teams/groups for this slice.
- Save logic serializes selected users into `submittedScorecardEditors.users` and sends empty `teams/groups`.
- Added a new template-builder access-step picker below "Who can use this scorecard".
- The picker is user-only, filtered by `permissions.scorecardGraders`, and clears all selections whenever grader roles change.

## Validation Notes

- `yarn biome check` passed on the edited files in the fresh Director worktree.
- Full `yarn workspace @cresta/director-app tsc` was not a reliable signal in this worktree because the repo currently has broader unresolved generated-client / workspace build issues unrelated to this change.
