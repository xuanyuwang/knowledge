# 2026-09-10

## Director frontend feature-flag rollout skill

- Created `enable-frontend-feature-flag` on branch `xw/enable-frontend-feature-flag` from `coaching-qm-skills` `origin/main` in an isolated worktree.
- Initially preserved Cresta Admin / Tenant Admin as the only mutation path, then corrected the design after code tracing showed the page calls ConfigService `BatchSetFeatureFlags` directly rather than triggering a GitHub Action.
- Revised the skill to generate the reviewed customer-history PR directly with a deterministic updater that reproduces ConfigService profile/use-case merge semantics from an explicit customer manifest.
- Defined final success as exact effective state after the merge-triggered non-dry-run forward sync and independent ConfigService-to-`configv3` plus Admin read-back.
- Captured protections learned from the CLO fleet rollout: semantic set comparison, explicit retry customer lists, cancellation detection, and canary-first recovery when customer history diverges from effective state.
- Skill Creator package validation and repository whitespace validation passed.
- Began applying the revised workflow to CONVI-7642: `enableNAScore=true`, staging then production, excluding Comcast and Schwab. Posted the approach and correction to the ticket; no customer config changed during the mechanism investigation.
- Opened [coaching-qm-skills#5](https://github.com/cresta/coaching-qm-skills/pull/5) with the direct-PR skill after package validation, Python compilation, semantic fixture testing, and idempotency testing.
- Exercised the skill against current config `master`: staging resolved to 29 customer files and 176 profile/use-case scopes, producing [config#153660](https://github.com/cresta/config/pull/153660).
- Rebased the skill branch onto current `main`, preserved the concurrent analytics-skill README entry, revalidated the package, and updated PR #5 to commit `c661cf4a99a66c87c847b2d439285e7f86690924`.
- CodeRabbit identified a valid partial-write risk in the updater. Commit `a628c689c64f17c458fc813baa5d3a746362a59d` stages the entire manifest in memory before writing and adds regression evidence that a late malformed customer leaves earlier files unchanged; CodeRabbit re-review is pending.
- CodeRabbit approved the fix and [coaching-qm-skills#5](https://github.com/cresta/coaching-qm-skills/pull/5) merged as `f90727177bbdef00f8844e88b19644d7a03fc401`.
- Addressed PR #5's fail-closed review finding in `a628c689c64f17c458fc813baa5d3a746362a59d`: all selected customer files are now validated and staged before any writes occur. Verified both atomic failure behavior and a successful two-customer batch, replied to and resolved the thread, and received CodeRabbit approval with all checks green and the PR cleanly mergeable.
- Production application exposed unrelated YAML `featureFlags` blocks and six customers with mapless email use cases. Hardened the updater to locate only profile/use-case frontend blocks and added an explicit profile-only application mode mirroring ConfigService `LEVEL_PROFILE_ONLY`.
- Opened [coaching-qm-skills#6](https://github.com/cresta/coaching-qm-skills/pull/6). Real production dry runs covered 356 all-level customers and six profile-only customers; the resulting config independently resolves 1,972/1,972 scopes to true.
- PR #6 is approved and cleanly mergeable with auto-merge disabled; human merge remains pending.
- Hardened PR #6 at `60766ae` after the production application showed that a green ConfigService dry run can still contain unrelated semantic changes. The skill now requires full artifact inspection and a flag-only effective diff, preserving exact current stored values only when customer-history representation drift would otherwise change unrelated configuration.
- Further hardened PR #6 at `d578564`: when stored drift belongs to a scope absent from customer history and cannot be preserved safely, the skill now requires isolating that customer, reporting the coverage gap, and using a separately reviewed patch-level path rather than accepting unrelated changes.
