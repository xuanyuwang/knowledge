# 2026-08-06

- Rechecked unfinished CONVI-7383 Phase A targets. `us-west-2-staging` is infrastructure-ready: failed pod recovered (`2/2 Running`, zero restarts), ClickHouse is reachable, and the cluster-wide distributed DDL queue has zero non-`Finished` rows.
- `ca-central-1-prod` remains blocked: TLS connection to ClickHouse `:9440` timed out after ~30 seconds, consistent with prior Code 209 failures.
- Confirmed `clickhouse-schema` `origin/main` remains at `69d6a05` with the Phase B backfill in `queries.sql`; Phase A DDL must be restored and pushed to `main` before running the staging apply. No GHA or DDL was run, and the Insights CLO MV flag remains off.
- Session: `sessions/2026-08-06/cursor-phase-a-blocker-recheck.md`.
- Verified PR #30588 golden CLO QA SQL on voice-staging `cresta_walter_dev`: raw vs MV filter sets identical; full score-stats aggregates match for `unresolved` over 30d; JSONHas numeric-bin guard prevents 36k false matches on string CLO template. Session: `sessions/2026-08-06/cursor-voice-staging-golden-sql-verify.md`.
