# Project Log - 2026-03-10

## Progress

- Rebased PR #16777 onto latest `origin/main`, resolving all merge conflicts.
- Force-pushed rebased branch. PR is no longer in CONFLICTING state.

## Details

### Rebase conflicts resolved

Main had added `scoreResource` to leaderboard filters and `tCommon` (common i18n) to Performance filters since the branch diverged. Conflicts appeared in 4 commits during rebase:

| Commit | File(s) | Resolution |
|--------|---------|------------|
| `[CONVI-6247] Add listAgentOnly filter key and state types` | `useLeaderboardsFilters.tsx` (5 conflicts) | Keep both `scoreResource` (main) and `listAgentOnly` (ours) — next commit reverts leaderboard anyway |
| `Fix: Add fallback for listAgentOnly from cached localStorage` | `useLeaderboardsFilters.tsx` (1 conflict) | Keep both `scoreResource` fallback and `listAgentOnly ?? true` |
| `[CONVI-6247] Add i18n for Agents Only filter labels` | `usePerformanceFilters.tsx` (2 conflicts), `director-app-insights.json` (1 conflict) | Keep both `t` (insights i18n) and `tCommon` (common i18n); drop leaderboard locale keys (reverted later) |
| `[CONVI-6247] Remove Leaderboard changes from Performance-only PR` | `useLeaderboardsFilters.tsx` (5 conflicts), `director-app-insights.json` (1 conflict) | Keep `scoreResource` from main, remove `listAgentOnly` (revert); keep leaderboard locale as main |

### PR review comments status

All existing comments are addressed or N/A:
- cursor[bot] `?? true` fallback in Performance `localStateToFilterState` — already fixed
- cursor[bot] `?? true` fallback in Leaderboard — N/A (file reverted from PR)
- cursor[bot] unmemoized `getFilterSelectionMenuOptions()` — N/A (reverted to static constant)
- coderabbitai `i18n.language` dependency — N/A (reverted to static constant)
- coderabbitai i18n consistency for all menu labels — out of scope (matches existing pattern)
- cursor[bot] HIGH filter vanishing on "No" — already fixed (`hasAValue` override)

### Pre-commit secret scanner false positive

Previous merge attempt was blocked by pre-commit hook flagging `IntegrationEnum.AWSS3` as a secret in `S3FolderSelection.tsx` (a file from main). Rebase avoided this because commits are applied individually and the hook only checks changed files in each commit.

## Next Steps

- Phase 4: Leaderboard page filter (separate PR)
- Phase 5: API pass-through wiring
