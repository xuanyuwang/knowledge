# 2026-08-21

- Verified that Spruce Spanish support is present on config `master` at profile and `voice-care` use-case levels after merged PR #152086, but its post-merge ConfigService sync run was cancelled.
- Identified the config collision: #152086 restored Spanish in the generated ConfigService diff while removing both Spruce `enableCLOFilters` entries from Git.
- Confirmed `disableEditingOnSubmittedScorecards` and `filterByScorecardSubmitTime` remain enabled for Spruce; only the CLO flag regressed.
- Confirmed production CLO PR #152031 and staging PR #152040 are merged; Spruce needs a targeted CLO re-add after Spanish runtime verification.
- Checked Ameer’s open Spruce PRs: #525 validates successfully, while #526 is currently blocked by an independent `lookup_consumers` tool-schema mismatch rather than Spanish.
- Initial history audit found 1,773 true / 233 absent / zero false in `customer_config_history`, including 116 unexpected gaps across 11 customers. Corrected the interpretation after comparing effective generated config and Admin state.
- Production GA sync run 32412308036 reported success with `diff_only=false` and 23 persisted batches, but Uber, Twilio, and eBay remain absent in current ConfigService-derived `configv3` despite true history entries. Admin's 249 disabled profiles is therefore the meaningful effective-state count; the 11-customer figure only measures later Git-history rewrites.
- Created isolated repair worktree `/Users/xuanyu.wang/repos/config-convi-7543-repair`, restored 116 missing eligible production values across 11 customers, and opened [config#152115](https://github.com/cresta/config/pull/152115). Validation found 1,889 true maps, zero false maps, and exactly 117 intentional Comcast/Schwab omissions; diff is 116 additions with no deletions.
- All #152115 checks passed, including ConfigService dry-run, schema/config consistency, YAML lint, credential scan, and Schwab no-change validation; the PR is open with a clean merge state.
- Resolved a later #152115 conflict by merging current `master` and preserving both the concurrent Marriott GC feature additions and `enableCLOFilters`. Concurrent updates had also introduced 64 additional eligible gaps across eight customers; the refreshed PR now contains 180 additions across 17 files, covers 1,891 eligible maps, has zero deletions, and remains at exactly 117 intentional Comcast/Schwab omissions. GitHub reports `mergeable: true`; refreshed checks are pending.
- Prepared an explicit replay list of 354 eligible production customers (3,817 characters), excluding all 17 Comcast/Schwab customer files.
- Session: `sessions/2026-08-21/codex-spruce-config-collision.md`. Work item: `work-items/CONVI-7543.md`.
