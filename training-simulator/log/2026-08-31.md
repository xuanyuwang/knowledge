# 2026-08-31

- **Milestone 1 gap revalidation:** Reconciled the August 24 review and August 26 remaining-work list with the accepted August 27 result-state decision. CONVI-7582 is superseded rather than unfinished; CONVI-7583 backend correctness, CONVI-7584 focused Director deltas, two contract decisions, and release validation remain.
- **Frontend scope corrected:** Removed distinct overall all-N/A presentation and missing-status warning work that existed solely to distinguish the now-collapsed zero/false cases. Retained attempt count, backend-owned classification, unavailable values, latest-activity semantics, routing/defensive joins, and focused tests.
- **Open decisions:** Partial timed-out snapshots with applicable/nonzero results and reporting persona/row-scope authorization still need explicit contracts.
- **Local evidence:** No CONVI-7583 or CONVI-7584 worktree or branch was found. Linear was not queried and remains authoritative for official status; CONVI-7582 cleanup and any stale dependency links remain tracking follow-up.
- **Evidence:** `sessions/2026-08-31/codex-milestone-1-current-gaps.md`, `work-items/CONVI-7583.md`, `work-items/CONVI-7584.md`.
- **CONVI-7583 backend plan:** Traced current proto, handler, database tables, tests, and Director consumption. The only new response datum is per-agent `attempt_count`. Existing status/pass/score/not-applicable fields and existing storage are sufficient, so no result-state field or schema migration is required.
- **Implementation boundary:** Planned assignment-rooted authorization, bounded direct task-run/outcome reads, deterministic `(created_at, resource_id)` official-attempt selection, conversation/quiz normalization, zero-run coverage, corrected unavailable values/score scale, stable ordering, structured stage telemetry, and scale/authorization tests.
- **Evidence:** `deliverables/convi-7583-backend-implementation-plan.md`, `sessions/2026-08-31/codex-convi-7583-backend-plan.md`.
- **Authorization correction:** A second Director consumer exists in agent-facing Assigned Training Sessions and uses the task-stats RPC for the assignee's progress, status, and score. Retained `AGENT`; the backend plan now requires self-only agent scope and manageable-user manager scope.
- **CONVI-7583 proto worktree:** Created `/Users/xuanyu.wang/repos/cresta-proto-convi-7583` from current `origin/main` on `xw/convi-7583-session-stats-contract`. Added only `AgentPerformanceEntry.attempt_count = 8`; the service authorization annotation is unchanged. Buf lint/build and `bazel build //cresta/v1/trainingsimulator:all` passed. The main cresta-proto checkout's unrelated local changes were untouched.
- **CONVI-7583 contract PR:** Committed `7d425251e5`, pushed the branch, and opened [cresta-proto#9716](https://github.com/cresta/cresta-proto/pull/9716) as a non-draft PR. Proto import/path lint and dependency checks also passed; initial CI is queued/in progress.
- **CONVI-7583 backend draft:** Created `/Users/xuanyu.wang/repos/go-servers-convi-7583` from `origin/main` commit `95068db42b` on `xw/convi-7583-session-reporting-backend`. Drafted assignment-rooted zero-run reporting, shared ACL/request user resolution with agent self-only narrowing, audience intersection, bounded direct GORM reads, batched outcome enrichment, attempt counting before latest reduction, deterministic tie-breaking, required-module filtering, and zero-plus-availability aggregates. Used pending `AttemptCount` directly. Per user request, ran no formatting, generation, build, lint, or tests.
- **CONVI-7583 draft scope narrowed:** Removed request user/group expansion, `direct_team_only`, row-level ACL, agent self-scope, audience intersection, and downstream authorized-user dependencies. The proof now aggregates each qualified task's full stored audience while retaining assignment-rooted zero-run rows, bounded tenant reads, outcome enrichment, attempt counting, latest selection, and required-module aggregation. These filtering/authorization capabilities are explicitly unsupported in the draft.
- **Milestone 1 FE data revalidation:** Compared current Director, the Figma session drawer, current proto/main, the CONVI-7583 contract branch, and live Linear state. The two remaining visible additions are a filtered-set incomplete count and per-agent drawer attempt count. Passed/Failed/Incomplete plus score and unavailable aggregate rendering already exist; “per-agent pass rate” is not a missing current-design datum.
- **Response derivability:** The incomplete count is derivable from Director's assignment audiences and agent-session statuses, though the backend must still become assignment-rooted to make the API independently complete. Historical attempt count is not derivable from latest-per-module `task_runs`; `AgentPerformanceEntry.attempt_count` in [cresta-proto#9716](https://github.com/cresta/cresta-proto/pull/9716) remains the only required new field. The PR is open, mergeable, and green.
- **Tracking update:** Linear confirms CONVI-7582 is Done, CONVI-7583 is In Progress, and CONVI-7584 is Backlog. Added the superseding decision to CONVI-7582, replaced CONVI-7583/7584 descriptions with the verified scope, removed the stale CONVI-7582 blocker, and verified CONVI-7583 still blocks CONVI-7584.
- **Follow-up verification:** Independent frontend, backend-contract, and knowledge-scope checks agreed that `attempt_count` is the only new response field. Figma resolves the dashboard incomplete grain as unique assigned agents with at least one incomplete visible session (`assigned - complete-in-every-visible-session`). Updated Linear and durable docs accordingly, corrected the stale “no worktree” snapshot, and added current-state notices to older Milestone 1 artifacts whose historical bodies still describe superseded N/A persistence.
- **CONVI-7583 backend redraft:** Implemented the focused query/aggregation change in `/Users/xuanyu.wang/repos/go-servers-convi-7583`: assignment-rooted zero-run rows, explicit audience-filter intersection, direct tenant-scoped task-run reads with a 100,000-row bound, chunked conversation/quiz enrichment, required-module attempt counts, deterministic latest-attempt selection, and regression tests for zero runs and newer incomplete retries. `gofmt` and `git diff --check` pass. Compilation passes with pending `AttemptCount` references temporarily excluded; final compilation awaits the cresta-proto#9716 artifact. DB test execution was blocked before test bodies by embedded PostgreSQL shared-memory exhaustion and by the local PostgreSQL server lacking the expected `cresta` role.
- **CONVI-7583 review-ready backend:** Upgraded the worktree to cresta-proto v2.21.27 and synchronized Go/Bazel dependencies. Polished latest-attempt selection, kept comments concise, and added coverage for time/user filter forwarding, task-audience intersection, zero-run assignments, latest incomplete retries, deterministic resource-ID ties, stale modules, mixed attempt counts, and 1,001 attempts. `mage cleanBuild`, full Go package tests, Bazel package tests, Gazelle, vet, formatting, and diff checks pass. Repository lint remains blocked by a Go 1.24-built golangci-lint against the Go 1.25 target. Changes remain uncommitted for review.
- **Empty-audience correction:** Verified `ListDirectorTasks` applies audience filtering only when parsed audience IDs are non-empty. Updated session aggregation to use the same convention: empty `audienceUserNames` means all assigned users, while a non-empty list is intersected with each task audience. Focused reporting tests and vet pass.
- **Attempt-count scope correction:** The required-module restriction came from the implementation plan rather than independent Product evidence. Updated the backend to count every task run within the assigned agent's session, including retries, incomplete runs, and stale-module runs; required modules still govern completion, score, pass, and returned official runs. Focused reporting tests and vet pass. The v2.21.27 proto comment still needs correction.
- **Task-run query simplification:** Removed task-ID chunking from `fetchTaskRunsForTasks`; reporting now requests one 100,001-row page as its overflow probe.
- **Task-run List reuse:** Extracted the List RPC body into `listTrainingSimulatorTaskRuns(ctx, req, pageSize)`. The public RPC passes 1,000 and stats passes 100,001. Reverted unrelated defensive changes to the existing outcome loaders. Focused Go tests, vet, formatting, and diff checks pass.
- **Latest-attempt filtering correction:** Removed `filterLatestAttempts`, which introduced assignment/current-module filtering not present in the original aggregation path. Existing metric helpers still limit completion, score, and pass calculations to current required modules; `task_runs` now retains the latest stale-module run. Updated the two new stale-module tests accordingly. Full Go package tests, vet, formatting, and diff checks pass.
- **Existing-behavior constraint audit:** Besides requested reporting changes, identified five review decisions still embedded in the draft: the 100,000-run ceiling, stricter score-plus-pass completion checks, whole-request failure for malformed DirectorTask names, deterministic task-run tie/order behavior, and first-configured-lesson selection. No product code was changed during this audit.
- **Constraint decision:** Retained only the 100,000-run ceiling and resource-name tie-breaking for equal timestamps. Restored score/submission-only settled detection, skip-on-malformed-task behavior, and unsorted task-run output. Run-backed tasks again derive lesson metadata from the first run; configured lesson lookup remains only as the zero-run fallback. Full Go tests, vet, formatting, and diff checks pass.
- **Comment restoration:** Restored the detailed `calculateTaskMetrics` assumptions with explicit separation between score/pass/completion and all-run attempt counting. Also restored explanatory comments for task-name construction, shared List loading, first-run lesson selection, latest-attempt grouping/sorting, audience-filter testing, and stale-module testing. Focused tests and diff checks pass.
- **CONVI-7583 backend PR:** Committed the implementation, rebased onto current `origin/main`, resolved dependency conflicts by retaining main's newer cresta-proto v2.21.28, and opened [go-servers#31780](https://github.com/cresta/go-servers/pull/31780). Post-rebase Go tests, Bazel package tests, Gazelle, vet, formatting, and diff checks pass.
- **Audience review finding:** Verified that `ListDirectorTasks.AudienceUserNames` uses `HasAny` to filter tasks but returns each matched task with its full audience. For a shared A+B task filtered to A, stats still loads both agents' runs by task/lesson; the current metric pipeline can therefore include B in task score and pass-rate denominator while returning only A in `AgentPerformance`. CodeRabbit's concern on go-servers#31780 is valid for explicit user/group filters.
- **Full-audience decision:** Restored the existing `ListDirectorTasks` semantic boundary: request users/groups select tasks, while every matched task's metrics and `AgentPerformance` use its full stored audience. Pushed commit `286c64633e` to [go-servers#31780](https://github.com/cresta/go-servers/pull/31780); full Go tests, vet, Bazel package tests, formatting, and diff checks pass.
- **Audience review thread:** Replied with the full-audience resolution and commit evidence, then resolved [discussion r3898767791](https://github.com/cresta/go-servers/pull/31780#discussion_r3898767791).
- **Scale review finding:** The 1,000-row List cap is insufficient for complete attempt counting, but the 100,001-row internal call still materializes and enriches the full set in the RPC heap. Existing enrichment uses single score-ID `IN` queries, creating a concrete parameter-limit risk near the ceiling. Recommended folding minimal paged rows into counts/latest state and enriching only retained latest runs; no product code changed.
