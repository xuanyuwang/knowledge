# 2026-09-11

- **Outcome:** Recovered the exact mapping between all seven oversized inline-image comments and seven distinct conversation IDs for CONVI-7671.
- **Impact:** The mapping exposed the immediate root cause: a single-scorecard export can receive profile-wide comments because it sends deprecated top-level conversation IDs while `ListComments` reads only the nested filter field.
- **Role:** Diagnosed and traced record-level evidence.
- **Evidence:** Preserved output from the successful 2026-09-10 read-only `app.chat_comments` query; mapping recorded in `sessions/2026-09-11/codex-list-comments-conversation-mapping.md` and `work-items/CONVI-7671.md`.
- **Follow-up:** Move export IDs to `filter.conversation_ids`, add a negative cross-conversation regression test, add the relevant comment-type filter, and retain byte-aware response protection. Refresh `voice-prod_ro` SSO before any current database recheck.
- **Ticket update:** Replaced CONVI-7671's description with the approved end-to-end call flow, exact UI and code locations, prioritized response-size mitigations, and an affected Director conversation link; verified the saved rich-text description after reload.
- **Guardrail finding:** There is no per-comment size limit in the proto, create/update validation, or database schema. Director explicitly permits base64 images in the shared editor used by ordinary conversation comments, so type filtering alone cannot prevent a single `CONVERSATION` comment from overflowing `ListComments`; retain image-exclusion and byte-aware handling in the fix.

## Credential use

- Rechecked and used only the explicitly selected `voice-prod_ro` AWS profile through the read-only customer app-database workflow. Its SSO session was expired, so no live database query ran. No credential material was printed or persisted.
