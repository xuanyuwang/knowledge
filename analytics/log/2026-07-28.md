# 2026-07-28

- Investigated CONVI-7385 Agent Leaderboard scorecard column vs drawer mismatch for CNG Agnes Long.
- Root cause: Agent drawer FE clears `voicemailMoment`, while column `RetrieveQAScoreStats` retains the voicemail exclusion; ClickHouse shows 2/3 conversations are voicemail.
- Added work item `work-items/CONVI-7385.md` and session note `sessions/2026-07-28/codex-convi-7385-column-drawer-mismatch.md`.
- Wrote implementation handoff plan: `deliverables/convi-7385-column-drawer-count-parity-plan.md` (background, FE fix steps, tests, docs to update, DoD).
- Created `/Users/xuanyu.wang/repos/director-convi-7385` from latest `origin/main` and staged the CONVI-7385 frontend parity fix without adding new tests.
- Extended the plan after auditing latest main: Manager column also omitted voicemail, so both Manager column and drawer now preserve `voicemailMoment`; CodeRabbit found no issues.
- Initial validation and commit attempts were blocked because the new worktree could not authenticate to private GitHub Packages; commit hooks were not bypassed.
- Promoted the drawer filter-inheritance invariant to Leaderboard and QA Score subdomain docs.
- Refreshed package authentication; `yarn install --immutable`, `yarn lint:precommit`, `yarn tsc`, and all commit hooks passed.
- Pushed commit `506de9db6f`, opened draft Director PR [#21214](https://github.com/cresta/director/pull/21214), and added the PR/validation summary to Linear CONVI-7385.
- Diagnosed Heartland's Behavioral Hints 79% vs 160% discrepancy: Assistance `RetrieveHintStats` divides distinct positive moment annotations by distinct sent hint actions. Production ClickHouse reproduced 215/134 = 160.4%; deduplicating followed events by action gives 106/134 = 79.1%. Source data is valid; the API aggregation mixes units and Director displays the invalid ratio. Performance separately uses `RetrieveQAScoreStats`, so the cross-page metrics/populations are not intrinsically equivalent.
- Promoted the investigation into a canonical `RetrieveHintStats` API reference and created an Assistance Insights subdomain covering callers, request/grouping shapes, backend flow, source tables, hint-type semantics, invariants, validation queries, and testing gaps.
- Investigated CONVI-7384 Coaching Hub QA mismatch. RCG production ClickHouse exactly reproduces all three observations: weekly Performance includes six `scorecard_time` rows through July 5 (91.6667%), monthly June includes four `scorecard_time` rows (93.75%), and the Scorecards tab includes five June `submit_time` rows (95%). Confirmed the REST route is `ListScorecards`, created the dedicated backend worktree, and added temporary request/query/bucket instrumentation for end-to-end boundary confirmation.
- Read-only investigation of ClickHouse conversations-cluster materialized view patterns in `clickhouse-schema`: init_db vs run_queries, POPULATE usage, TTL (only delete queue / request_log), DROP+_d ordering from INSI-1073, no 180-day backfill pattern in-repo. Session: `sessions/2026-07-28/codex-ch-mv-patterns.md`.
