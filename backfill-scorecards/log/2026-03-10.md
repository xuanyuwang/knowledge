# Project Log - 2026-03-10

## Progress

- Reviewed and addressed all PR comments on go-servers #25916
- Rebased onto latest main (cresta-proto v2.0.634 → v2.0.646)
- Cleaned up unrelated files accidentally included during rebase
- Squashed cleanup commits (17 → 13 commits)
- Fixed test broken by new proto validation

## Details

### PR #25916 Comment Review

**CodeRabbit comments (14 total):**

| Comment | Status | Action |
|---------|--------|--------|
| Reject unsupported REINDEX_MODE | Already addressed | Replied + resolved (switch/default) |
| Validate time range in parseTimeRange | Already addressed | Replied + resolved (endBefore check) |
| HeartbeatDetails type tracking | Not applicable | Replied — removed struct, no resume by design |
| Negative ScheduleToClose timeout | Already addressed | Replied + resolved (min 10m bound) |
| Guard jobName nil | Already addressed | Replied + resolved (nil check added) |
| Consistent error prefix | Updated | Changed to `BatchReindexScorecards` for process scorecard method |
| startTime < endTime validation | Already addressed | Replied + resolved (in buildReindexConfig) |
| Zero conversationStartedAt | Already addressed | Previously replied — process-only workflow |
| Batch size validation | Already addressed | Replied + resolved (batchSize <= 0 check) |
| Use status.Error for CanScheduleJob | Fixed | Changed `fmt.Errorf` → `status.Error(codes.PermissionDenied, ...)` |
| REINDEX_MODE=all partial-success | Acceptable | Replied — UUID workflow IDs + idempotent CH writes |

**tinglinliu comments (4 total):**

| Comment | Status | Action |
|---------|--------|--------|
| Batch reads for director scores | Already addressed | Replied — `fetchDirectorScoresBatch` does batch query |
| Template parsing/caching | Already addressed | Replied — `templateRevisionCache` keyed by templateID:revision |
| Backfill all templates when no filter | Already addressed | Replied — default is all templates, filter only when explicitly provided |
| Process scorecards only | Already addressed | Replied — scoped to process only, convo uses reindexconversations |

### Code Changes

1. `reindex_scorecards_handler.go`: `CanScheduleJob` → `status.Error(codes.PermissionDenied, ...)` instead of `fmt.Errorf`
2. `task.go`: Error prefix `BatchReindexConversationsTask` → `BatchReindexScorecards` in `runProcessScorecardsReindex`
3. `action_retry_job_test.go`: Fixed `NewJobId` to conform to new proto regex `^[a-z0-9][a-z0-9-]{6,62}[a-z0-9]$` (lowercase + length cap)
4. Removed stray files from rebase: `example_request.json`, `example_response.json`, `outcome_direction.go/test`
5. Ran gazelle to update BUILD.bazel files
6. go.mod/go.sum updated to cresta-proto v2.0.646 from main
