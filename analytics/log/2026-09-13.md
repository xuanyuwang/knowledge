# Analytics Log - 2026-09-13

- Posted the CONVI-7667 investigation report to Linear through the authenticated Linear MCP (comment `df73e6fb-740a-4a29-a46c-308bf595b265`).
- Reported the confirmed request-construction root cause, PostgreSQL/ClickHouse agreement, exact zero-result reproduction, and the narrow Director fix with regression coverage.
- Explicitly advised against a scorecard backfill because the required July 2025 and July 2026 rows are already present and scorable in ClickHouse.
- Created Director worktree `/Users/xuanyu.wang/repos/director-convi-7667` on branch `convi-7667-process-scorecard-date-target` from current `origin/main`.
- Implemented process-template filter normalization that clears the conversation-only `dateRangeTarget`, with preservation coverage for conversation templates.
- Passed the focused Vitest suite (4 tests), Director-app TypeScript build, and commit-hook lint/i18next/format checks.
- Pushed commit `961a44a922` and opened draft Director PR [#22772](https://github.com/cresta/director/pull/22772). Declared `smoke-functional` QA with concrete steps and expected behavior; preview before/after videos remain pending.
- Read Support comment `40b9e1d6-4bca-4758-9c59-682e55358768` through the authenticated Linear MCP and checked the exact template in the user's live Oportun Chrome session.
- The current UI state did not reproduce zero data: July 2025 rendered 99% / 353 scorecards and July 2026 rendered 100% / 335 scorecards. This contradicts a template-wide data gap but, by itself, does not establish why the customer session failed.
- The dedicated browser-control surface was unauthenticated (`Codex auth token is unavailable`); native Chrome accessibility provided the rendered values but not network request inspection.
- Analyzed `/Users/xuanyu.wang/repos/oportun.cresta.com.har` without exposing auth headers or cookies. Of 14 successful `qaScoreStats:retrieve` calls, 12 omitted `conversationTimeRangeField` and returned populated results; two count-chart auxiliary calls sent `CONVERSATION_ENDED_AT` and returned zero.
- Tightened draft Director PR #22772 in commit `e392f523f3` so its internally reconstructed unfiltered and delta process-count requests also pass through process normalization. The two focused suites now pass 6 tests, and TypeScript/lint checks pass.
- The working HAR confirms the field's zero-result mechanism but not the original blank page's exact request path. A HAR from a failing session remains necessary for end-to-end root-cause confirmation.
- Verified the HAR's deployed Director build as `58a0ca6dcb` from 2026-09-10. Neither that build nor current `origin/main` contains a process/non-email date-target fix, so the live populated UI is not explained by a recent merged deployment.
- Found overlapping unmerged commit `8c12fa4296` on `origin/sdey/insi-4748-date-target-default` from 2026-09-11. It clears defaults/restored cache for non-email tenants and fixes the count-chart reconstruction path, but is in neither production nor main and has no discoverable PR.
