# 2026-07-30

- Investigated Coaching Hub and QM Report scorecard completion APIs vs Manager Leaderboard for CONVI-7162. Both Hub and QM count by Postgres `submitted_at`; Leaderboard QA path defaults to ClickHouse `scorecard_time`. Deliverable: `deliverables/coaching-hub-qm-report-submit-time-investigation.md`.
- Captured Tinglin Liu’s Slack decision to restore Manager Leaderboard to scorecard submit time: original pre–CONVI-6968 behavior was submit time; conversation time came from the QA API migration. Decision: `decisions/2026-07-30-manager-leaderboard-submit-time.md`.
- Opened go-servers PR for `TimeRangeFilterTarget=SUBMIT_TIME` wiring on QA scorecard ClickHouse path; bumped `cresta-proto` to v2.17.3 (field renamed from `ScorecardTimeBasis` via proto #9430).
- Landed cresta-proto PR [#9402](https://github.com/cresta/cresta-proto/pull/9402) as `dfc39d93ae`: accepted review naming/deduplication feedback in `e2ec849063`, resolved both threads, and confirmed the prior training-simulator `QuizTemplateName` failure was unrelated. Full CI, including go-servers `go generate`, passed; no generated files were committed. Session: `sessions/2026-07-30/cursor-proto-pr-landing.md`.
- Merged follow-up proto PR [#9430](https://github.com/cresta/cresta-proto/pull/9430) as `f4ebb13e31`: renamed `scorecard_time_basis` → `time_range_filter_target` (with `allow-breaking-changes` for intentional field/json_name breaks). Proto surface is now `TimeRangeFilterTarget` + `time_range_filter_target`.
- Saved before/after time-range → ClickHouse column mapping writeup (mermaid diagrams + side-by-side Manager request): `deliverables/qa-time-range-column-mapping.md`.
