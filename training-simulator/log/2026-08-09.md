# 2026-08-09

## Created Training Simulator domain

- **Outcome:** Established `training-simulator` as a domain project seeded from Glean-fetched project documents (Trainings Simulator Design by Jack Jee / Kevin Kiernicki, PRD, Assignment Flow, launch/rollout docs) plus authoritative local inspection of the merged `cresta-proto` proto surface, `go-servers/apiserver/internal/trainingsimulator`, and `director` FE feature/hook code.
- **Impact:** Gives ongoing Training Simulator CONVI work (permission hardening, quiz P1, GA readiness) a canonical engineering home with 5 subdomains (content config, assignment/session, simulation runtime, evaluation, reporting) and a source map across go-servers / director / cresta-proto / python-ai-services.
- **Role:** Domain authoring + synthesis — reconciled draft design-doc protos against the merged service proto (which differs meaningfully: batch scenario CRUD, `EvaluateTrainingConversation` polling/statuses, `RetrieveTrainingSimulatorTaskStats`), and captured operational quirks (GoWalter role-swap, async eval polling, nil-score / lesson-required-module stats guards, `enableTrainingSimulator` flag + interim role gating).
- **Evidence:** [session notes](../training-simulator/sessions/2026-08-09/claude-training-simulator-domain-setup.md), domain [README](../training-simulator/README.md) + subdomains, `project.yaml`.
- **Follow-up:** Add CONVI-7145 permission work as first work item; refresh subdomains as quiz P1 and Synthetic Customers ship.

## Verified domain claims against code; refined three docs

- **Outcome:** Checked the eight user-supplied concept claims against the merged protos, `go-servers/apiserver/internal/trainingsimulator/` (evaluator + stats), and `director` FE simulation store. Confirmed most were documented; corrected that **the session/assignment is the DirectorTask, not the lesson** (claim 8 was wrong), and made explicit that **FE picks the random scenario** and that **passing a module requires passing a single scenario attempt** (claim 4).
- **Impact:** Domain now matches implementation, not just design docs: `pickRandomScenario` in `lessonUtils.ts`, single-scenario `TrainingSimulatorTaskRun`, latest-attempt module scoring, `RetrieveTrainingSimulatorTaskStats`, and the Coaching Plan `assigned-training-sessions/` surface are all reflected in the docs.
- **Role:** Code-grounding of domain docs.
- **Evidence:** README + `subdomains/training-content/README.md` + `subdomains/assignment-and-session/README.md` edits this day.
