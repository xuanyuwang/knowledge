# 2026-05-14

- Bootstrapped the `CONVI-6842` knowledge project for the Holiday Inn Performance Insights vs Closed Conversations mismatch.
- Resolved `go-servers` as the primary source repo and noted `director` as the secondary surface.
- Created dedicated worktree `/Users/xuanyu.wang/repos/go-servers-convi-6842` on branch `convi-6842-holiday-inn-voice-and-transfer-performance-insights-shows`.
- Captured the initial problem statement, working model, and first investigation areas in the project README.
- Narrowed the frontend side into two separate behaviors:
  - a transient `No template -> template -> No template` path where stale template-scoped QA data can remain visible in `ConversationCountChart`
  - a refreshed `No template` path that still fires a time-range `RetrieveQAScoreStats` request with `criterionIdentifiers: []` and `scoreResource: QA_SCORE_RESOURCE_SCORECARD_SCORE`, likely from the default performance-score widgets rather than the conversation-volume card
- Narrowed the backend investigation to the no-template path and confirmed the main mismatch is semantic, not transient UI state.
- Reproduced live ClickHouse populations for April 15, 2026, `transfers-voice`:
  - `RetrieveConversationStats`: `7253`
  - `RetrieveQAScoreStats(includeNaScored=false)`: `7256`
  - `RetrieveQAScoreStats(includeNaScored=true)`: `8284`
- Verified that the `1031` conversations in `QAScoreStats(includeNaScored=true) - ConversationStats` have no rows at all in `message_d`.
- Verified that the residual `3` conversations in `QAScoreStats(includeNaScored=false) - ConversationStats` also have no rows in `message_d`.
- Profiled the extra QA-only conversations from `conversation_d`:
  - all exist in `conversation_d`
  - most are near-zero-duration conversations
  - `876/1031` have duration `<= 0`
  - `931/1031` have duration `<= 1`
  - `986/1031` have duration `<= 5`
