# 2026-05-19

- Reframed the customer report from "coaching submission error" to "process scorecard failed to update on creation".
- Traced Director flow and confirmed the failing user-facing toast comes from `UpdateScorecard`, not `SubmitScorecard`.
- Matched API logs showing gRPC `code=5 NOT_FOUND` for `UpdateScorecard` to the backend `dao.GetScorecards(...)` lookup.
- Verified `s.appsDB.DB(ctx)` does not force primary reads and can route reads to replicas through GORM `dbresolver`.
- Identified the issue as read-after-write inconsistency due to replication lag.
- Implemented a narrow backend fix: force only the initial `UpdateScorecard` scorecard lookup to read from the write DB.
- Opened PR: https://github.com/cresta/go-servers/pull/27934
- First version of the fix was too broad and caused CI failures in `TestScorecardAsyncOrder`; narrowed the fix to the single lookup and force-pushed the branch.
