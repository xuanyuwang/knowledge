# Project Log - 2026-03-19

## Progress

- PR #17394 merged after addressing review comments (simplified `hiddenFilters` to module-level consts, fixed import ordering lint)
- BE PR #26301 merged
- FE PR #17356 confirmed deployed to prod — `filter_to_agents_only` now present on all requests including `RetrieveQAScoreStats`
- All PRs merged — project ready for staging E2E test with `enableAgentOnlyFilter` flag

## Details

### Review feedback on PR #17394

Addressed reviewer comment: replaced `useMemo` for `hiddenFilters` with two module-level constants (`DEFAULT_HIDDEN_FILTERS` and `AGENT_ONLY_HIDDEN_FILTERS`), selected based on feature flag. Simpler than Performance/Leaderboard which need `useMemo` due to multiple conditions.

Replied to CodeRabbit comment about missing `BooleanFilter` component — intentional design, filter uses level select pattern in "+Filters" dropdown, consistent across all three pages.

### Deploy status

| PR | Deployed to prod? |
|---|---|
| FE #16777 | Yes |
| FE #17314 | Yes |
| FE #17356 | Yes (confirmed Mar 19) |
| FE #17394 | No — next release cycle |
| BE #26301 | Pending BE deploy |

## Next Steps

- Test on staging with `enableAgentOnlyFilter` flag enabled
- Prod deployment of #17394 (next Tuesday cut → Thursday deploy)
- Enable feature flag for customers
