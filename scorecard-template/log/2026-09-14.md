# 2026-09-14

- Narrowed the CONVI-7663 Director patch from all known permission-hook consumers to Closed Conversations scoring call sites only.
- Retained scope propagation through `ScorecardTab`, `ScorecardFormLoader`, `useConversationCoachingDetails`, `ScorecardForm`, and the conversation-specific `ResetButton` props.
- Restored filter, Coaching Hub, admin, AI Agent, and Opera call sites to their original behavior.
- Validation passed: Director TypeScript, changed-file lint, and 2 focused permission-request tests.
- Rebasing onto current `origin/main` completed without conflicts; committed as `49e6af05e5` and opened draft Director PR [#22809](https://github.com/cresta/director/pull/22809).
- Verified and fixed a valid review finding: skipped permission lookups previously suppressed the request but still returned all permissions. Commit `ecb8f70c18` now fails closed only for `skip`, with ordinary loading remaining permissive; lint, TypeScript, and 3 focused tests pass.
- Verified and fixed a second valid review finding: missing `usecaseNames` had been conflated with an explicitly pending conversation scope. Commit `1efbdb7431` restores default unscoped behavior for omitted scope and adds an explicit Closed Conversations waiting-state signal; lint, TypeScript, and 5 focused tests pass.
