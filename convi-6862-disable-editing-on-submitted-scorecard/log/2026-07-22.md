# 2026-07-22

- Investigated a Walter-dev `UpdateScorecard` 403 for a requester listed in the latest template's submitted editors.
- Identified inconsistent template revision selection: proactive `EvaluateScorecardsPermissions` uses the latest template revision, while `UpdateScorecard` and `ResetScorecard` enforce against the scorecard's pinned historical revision.
- Confirmed proactive evaluation returned `allowed=true`, proving the latest-revision flag, permission parsing, user expansion, and requester match succeeded before the pinned-revision update path denied.
- Identified a secondary Director fail-open edge where a permission-query error leaves `allowed` undefined and the submitted scorecard form editable.
- Recommended using latest permissions consistently for submitted-editor authorization while retaining the pinned revision for scoring semantics.
- Chose the proposed implementation boundary: make `hasSubmittedScorecardEditPermission` load the latest template internally, remove its caller-provided template argument, and reuse it from `UpdateScorecard` and `ResetScorecard`.
- Created and populated [CONVI-7350](https://linear.app/cresta/issue/CONVI-7350/refer-the-latest-revision-of-template-for-permission-evaluation) with the root cause, implementation outline, acceptance criteria, and revision-mismatch regression plan.
- Captured the detailed trace and solution in `work-items/CONVI-7350.md` and `sessions/2026-07-22/codex-latest-template-permission-evaluation.md`.
- Implemented CONVI-7350 in `/Users/xuanyu.wang/repos/go-servers-convi-7350` on `convi-7350-refer-the-latest-revision-of-template-for-permission`.
- `hasSubmittedScorecardEditPermission` now loads `LatestRevisionName` internally; `UpdateScorecard` and `ResetScorecard` no longer pass the pinned template into authorization.
- Added allow/deny revision-mismatch regression tests for update and reset; focused and full update/reset/evaluate coaching suite filters passed.
- Opened [go-servers #30336](https://github.com/cresta/go-servers/pull/30336) (`3d580bd3b9`).
- Registered `go-servers-convi-7350` and `go-servers-convi-7206` worktrees in `workspace/repos.yaml`.
