# 2026-08-28

- **Module count review:** Reviewer feedback favors `passed_count` plus an assignment-rooted `total_count` over a passed/failed partition, with one total shared by module criteria. Proposed semantics count incomplete and N/A facts as denominator non-passes; historical missing-current-criterion behavior remains an explicit assumption to confirm. Group/team reporting filters are no longer required.
- **Count contract clarified:** Do not expose `failed_count` or redundant `pass_rate`. `total_count - passed_count` is an intentionally broad non-pass remainder containing failures and other states; criterion counts share the parent module total.
- **Proto changes published:** Pushed module count/filter updates to #9676 as `6b11a998c6` and lesson filter removal to #9677 as `cd21847e66`. Replied to reviewer threads `r3875681846` and `r3875693048`, then resolved both. Local Buf/Bazel/whitespace checks passed, and #9676 authoritative API lint passed.
- **Lesson API lint reconciled:** #9677's full API linter required consistency with the pre-existing unannotated `ListTrainingLessonsRequest` fields and an AIP-132 exception for the intentionally parallel `lesson_stats` list. Removed only the new `OPTIONAL` field-behavior annotations, documented the exception, validated the scoped Buf/Bazel build, and pushed `2733d8005b`.
- **Shared module stats adopted:** Per #9677 review, replaced `TrainingSimulatorModulePassRate` with `TrainingSimulatorModuleStats`, stacked #9677 on #9676, and updated the PR description. The lesson chips now use the shared passed/total contract. Pushed `99dfc72d37`, replied to `r3875814042`, and resolved the thread.
- **Lesson filter thread closed:** Verified `stats_virtual_group_names` and `stats_team_group_names` are absent from #9677 and its stacked diff, replied to `r3875821991` with the existing removal commit `cd21847e66`, and resolved the thread. Only `stats_user_names` remains, scoped to the reporting projection.
- **go-servers companion verified:** Reproduced #9676's synthetic merge-ref generation in a clean go-servers worktree. No Training Simulator generated diff was produced; the failures were the unrelated `SCREEN_ACTIVITY` and `ConversationChannel` incompatibilities already addressed by go-servers #31683. Cross-linked #9676 and #31683, documented the finding in the proto PR body, and checked the readiness acknowledgement instead of creating a duplicate PR.
- **Reporting proto PRs refreshed:** Rebased #9676 onto cresta-proto `origin/main` at `8453649516` and rebuilt stacked #9677 directly on the new #9676 head. The module patch stayed identical; the lesson branch now has one clean lesson-only commit with the exact reviewed final diff. Targeted Buf and Bazel validation passed and fresh CI runs started.
- **Reporting filter rationale documented:** Clarified in the backend design and Superhuman draft that module names select content while assignee/date fields select counted results. The server paginates content first and performs one bounded statistics load for returned page IDs; module IDs cannot encode the selected users or date range.
- **API direction changed:** The `private-qm-coaching` review established that avoiding a second frontend call/cold tab switch was the main reason for extending the content-list APIs, while dedicated stats RPCs provide the cleaner, conventional, discoverable boundary. Because the extra batch request is not a major performance concern, selected `RetrieveTrainingSimulatorLessonStats` and `RetrieveTrainingSimulatorModuleStats`; recorded the decision and generated focused prompts to update #9677 and #9676.
- **CONVI-7601 contract reworked locally:** Restored `ListTrainingModules` byte-for-byte to its pre-PR content contract and added bounded, ordered `RetrieveTrainingSimulatorModuleStats` with admin/reporting-only authorization. Adopted assignment/result-specific count names, a non-optional `average_applicable_score` paired with `applicable_score_count`, criterion-specific applicable denominators, and deterministic failed-result-first ordering. Gazelle, Buf, targeted Bazel, and whitespace validation passed; the private-image API lint could not run because ECR returned 403 and no AWS credential was cleared. The local commit remains unpushed for human review; #9676 metadata/replies are deferred until approval.
- **Module-by-agent grouping checked:** Current Figma shows one aggregate row per module and a module drawer with aggregate pass/score/criterion fractions; it does not show `(module, agent)` grouped statistics or an assignee-population filter. Agent identity appears only in the single-module CSV/result-row export annotation. Historical `(task, lesson, module, agent)` language describes the internal latest-result grain and does not justify a public grouping/filter field. Removed `agent_user_names` from the local CONVI-7601 request; it now accepts only module names and a time range.
- **CONVI-7601 branch published:** After local approval, force-pushed the rewritten single-commit module contract to #9676 at `1c22d8085293ca8626b7111e9097102af8fb84db`. Local and remote branch heads match; PR metadata and remaining review-thread responses are separate follow-up work.
- **Criterion contract refined locally:** Added `non_applicable_result_count` to `TrainingSimulatorCriterionStats`; it counts explicit completed criterion N/A results while remaining outside the applicable/pass-rate denominator and failed-result ordering. Removed unnecessary `behavior_name` because module name plus criterion ID already identifies a criterion, renumbered the unreleased fields contiguously, and updated stable ordering. Gazelle, Buf lint/build, targeted Bazel build, and whitespace checks passed; the follow-up is not pushed.
- **Request validation annotations simplified locally:** Removed every `validate.rules` annotation introduced by #9676 while retaining resource references, `REQUIRED` field behavior, and the documented 1–200/unique/nonempty module-name contract. Existing validation annotations outside the PR remain unchanged. Buf lint/build and whitespace checks passed; the amend is not pushed.
- **Latest CONVI-7601 amend published:** Force-pushed the locally approved criterion/request refinements to #9676 at `97873e3108a0cc1366f1bcd3868c0c802103b7c6`. Local and remote branch heads match.
- **PGV review thread reconciled:** Addressed CodeRabbit thread `r3885167462`. Acknowledged that generated validators will not enforce the documented request constraints, explained the explicit decision not to introduce `validate.rules` in #9676 and the resulting handler-validation responsibility, then resolved the thread without changing code.
- **CONVI-7600 rework prompt refreshed:** Produced a focused prompt to rebuild stacked lesson proto PR #9677 onto final module head `97873e3108`, restore `ListTrainingLessons`, add a dedicated ordered batch RPC, align lesson counts/score availability with the final module naming, inherit removal of agent filters/validation annotations/criterion behavior identity, preserve criterion N/A counts, and keep the lesson-only diff and push gated on local review.
- **CONVI-7600 contract reworked locally:** Rebuilt #9677 as one lesson-only commit (`6fa5404349`) on final #9676 head `97873e3108`, restored `ListTrainingLessons`, and added the admin-only ordered batch `RetrieveTrainingSimulatorLessonStats` API. Lesson results now expose distinct-session count, passed/total assignment counts, normalized average plus availability count, and ordered shared module stats; obsolete optional rate/average and missing-status fields are gone. Gazelle, repository formatting, Buf lint/build, targeted Bazel build, and whitespace validation passed. At local handoff nothing had been pushed, and the updated PR description remains a local draft.
- **CONVI-7600 branch published:** After local review, force-pushed the rewritten lesson branch with an explicit lease from old remote head `37608b3499` to `6fa5404349`. The PR description and review threads remain unchanged pending separate follow-up.
