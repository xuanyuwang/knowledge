# 2026-07-28

- Opened cresta-proto PR [#9400](https://github.com/cresta/cresta-proto/pull/9400) adding default-off Insights flag `use_conversation_outcome_moment_annotation_materialized_view` (field 30) to both mirrored Insights messages; config schema sync blocked until proto merges/releases.
- Handoff check-in on [#9400](https://github.com/cresta/cresta-proto/pull/9400): proto-contract + default-off verified; config schema sync still blocked. Related ticket [CONVI-7383](https://linear.app/cresta/issue/CONVI-7383/improve-clo-conversation-outcome-filter-query-performance-via). PR mergeable but blocked on pending `@cresta/core` codeowner review and remaining gen CI; do not merge/sync/enable yet.
- Made the longest `qaScoreStats:retrieve` request per page/filter iteration the primary UI performance proxy.
- Added matching-request count and click-to-stable-render as companion metrics and clarified that concurrent request durations must not be summed.
- Confirmed browser automation is available, but no Performance Insights tab was open and no measurement was executed.
- A later Chrome attempt found the NCLH Performance Insights tab, but site-specific browser security settings prohibited automation for `nclh-us-east-1.cresta.com`; control was released without interacting with the page.
- Prepared a read-only direct ClickHouse measurement with a 120-second timeout and concurrency 1. The production connection did not execute because the escalation approval service failed before AWS/ClickHouse access; explicit retry approval is pending.
- After explicit approval, completed preliminary NCLH production measurements. The closest full-query comparison showed 85.83 s with CLO versus 66.67 s without CLO, with approximately 10.1× rows and 7.5× bytes.
- The direct CLO annotation component took 17.83 s and read 2.017B rows / 245.4 GB. JSON filtering increased the paired count from 0.467 s to 3.400 s, bytes by 3.02×, and shard user CPU by 2.29×.
- `EXPLAIN indexes = 1` showed the time/type indexes still selected 90.9% of local granules. No production writes or unsafe cache operations were performed.
- Results: [CLO filter production results](../deliverables/clo-filter-performance-results-2026-07-28.md).
- Diagnosed cresta-proto #9400's `Check go-servers go generate` failure as unrelated stale EmailConfig and Moment converter contracts exposed by repository-wide generation. go-servers #30519 merged after the failed run and fixed both mismatches, so #9400 should only need a CI rerun; the mirrored Insights flag itself does not require a companion implementation PR.
- Investigated ClickHouse MV creation patterns for the CLO view (CONVI-7383): Cresta historically uses `POPULATE`; ClickHouse docs prefer create-without-populate + chunked `INSERT SELECT` for live tables. Recommended 180-day chunked backfill, off-peak rollout, and no TTL by default. Report: `deliverables/clo-mv-clickhouse-creation-investigation.md`.
