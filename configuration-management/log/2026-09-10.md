# 2026-09-10

- Traced Director feature-flag bulk editing from Tenant Admin to ConfigService for CONVI-7642.
- Confirmed the Admin page calls `BatchSetFeatureFlags`; it does not trigger a GitHub Action for existing-customer enablement.
- Documented the handler's wildcard scope, profile/use-case merge behavior, `CREATE_PR_TO_REVIEW` path, and the non-equivalence of the existing add/docs/read-back workflows.
- Updated the reusable `enable-frontend-feature-flag` skill to support direct reviewed PR generation from an explicit customer manifest while retaining forward-sync and effective read-back verification.
- Refreshed config `master` over HTTPS and created isolated worktree `/Users/xuanyu.wang/repos/config-convi-7642-staging` on `xw/convi-7642-enable-na-score-staging`; the dirty main checkout remained untouched.
- Resolved CONVI-7642 staging to all 29 current staging customer identities; Comcast and Schwab had no staging identity matches. Added `enableNAScore: true` to 176 profile/use-case scopes.
- Verified the exact 176-addition-only diff, updater idempotency, and the repository ConfigService-history schema validation; opened [config#153660](https://github.com/cresta/config/pull/153660) and posted the status to Linear through MCP.
- Attached the quiet `CONVI-7642 flag rollout` monitor at a 30-minute cadence. It comments on Linear only for meaningful lifecycle transitions, never merges automatically, and blocks production until exact staging effective-state verification.
- Staging PR #153660 merged after all PR checks passed. Recorded merge commit `d7dce7a2` and active non-dry-run Batch Sync run #34504377738 on CONVI-7642 through Linear MCP; production remains blocked pending forward sync and effective read-back.
- Forward Sync run #34504377738 succeeded without cancellation; its artifact contains exactly 48 profile and 128 use-case changes. Read-back PRs #153672 and #153675 merged, confirming 83/176 effective scopes across six customers. Posted **forward synced / read-back pending** to Linear; 93 scopes across 23 customers and staging smoke testing still block production.
- Ran the rollout monitor on demand. Read-back run #34517259670 succeeded but skipped staging and auto-merged unrelated production-only PR #153691. The exact staging result is unchanged at 83/176 scopes across 6/29 customers, so production remains blocked and no duplicate Linear comment was posted.
- The user accepted prior QA staging testing and authorized production review in parallel with the incomplete staging read-back. Resolved 380 production customer files to 362 included identities and 18 Comcast/Schwab exclusions, including aliases `cmcst` and `cresta-sandbox`.
- Found six included customers whose mapless email use case makes ConfigService `LEVEL_ALL` reject the entire customer. Used a separate profile-only fallback for those customers and verified inheritance across all 1,972 intended production scopes.
- Opened [config#153693](https://github.com/cresta/config/pull/153693): 362 files, 1,955 additions of only `enableNAScore: true`, zero deletions, exact exclusions preserved, schema validation passed, and auto-merge disabled. Posted the production scope and review state to CONVI-7642 through Linear MCP.
- All required production PR checks passed, including the `diff_only=true` Batch Sync to ConfigService. Posted the transition to CONVI-7642; PR #153693 remains unmerged with auto-merge disabled and needs human review.
- Inspected the complete successful production dry-run artifact and found 15 unintended removals of explicit empty `summarization_config` values caused by empty-versus-absent customer-history drift. Reclassified PR #153693 as **unexpected diff / do not merge as-is** and recorded it on Linear.
- Updated PR #153693 at `a3eedf33aa5cb125d5ea7472972088aa960ccbc7` with exact `summarizationConfig: {}` preservation entries for those 15 use cases. Schema validation passes; replacement dry-run #34525294546 must prove the effective diff is flag-only before review resumes.
- Replacement dry-run #34525294546 eliminated the 15 known removals but full changed-line inspection found a sixteenth under The Zebra's stored, absent-from-history `ai_poc` use case. Isolated The Zebra from PR #153693 at `82720e96731c97c23aa12388d4e4fe216827469f`; the PR now targets 361 customers and 1,967 scopes, with The Zebra's five scopes tracked as a separate patch-level rollout gap. Posted the transition to Linear.
- Dry-run #34526815160 exposed concurrent-master deltas, so rebased PR #153693 onto current `master`. Final run #34528022370 passed with exactly 1,950 `enableNAScore: true` effective additions and no configuration removals or unrelated changes. All checks pass; recorded **ready for review for the safe cohort** on Linear. Auto-merge remains disabled.
- A later `master` rebase duplicated 13 preservation keys and broke both JavaScript YAML checks. Removed the duplicates, rebased to `23df779e`, and adopted a strict source-and-effective flag-only rule rather than carrying any preservation fields.
- Final artifact #34533387320 proves exactly 1,949 `enableNAScore: true` additions and zero removals or unrelated changes across 360 customers. `cresta-testing-ca` and The Zebra were isolated because each would expose unrelated stored summarization drift.
- Merged production PR #153693 as `4264155cd9bb8bd2b1261068ceccc69c022cd1d2`; forward-sync run #34534431100 is queued. Recorded the transition on Linear and updated the rollout monitor to track the 1,963 effective-scope safe cohort plus both explicit gaps.
- Forward run #34534431100 was cancelled by a newer master push before regional sync. Succeeding non-dry-run run #34534865721 covered `52890e59..25ed8522`, including the rollout merge, and succeeded; its full artifact contains all 1,949 expected `enableNAScore: true` additions and zero flag removals. Recorded **forward synced / read-back pending** on Linear.
- Production Sync From ConfigService run #34536172681 auto-merged PR #153726 for the final six dispatched customers. Exact inheritance-aware comparison confirms 6/1,963 scopes across 6/360 customers, no explicit false values, and no fully read-back customer yet; posted the partial progress to Linear.
- Traced the partial read-back against the earlier CLO fleet recovery. Repository-dispatch runs cancelled one another because they shared a concurrency group, while manual read-back dispatches can use unique branch names to remain independent and produce non-auto-merged review PRs. Recorded a canary-first recovery using the six already profile-only customers: flag-only forward preview, explicit replay, unique-branch read-back, then small reviewed preview/persist/read-back batches for the remaining safe cohort.
- Read-back run #34544418123 targeted `marriott-sbx` and auto-merged PR #153728. It confirmed only the profile-level `enableNAScore` value out of five expected scopes and also added unrelated `enableRegexKeywordMoment`; production is now 7/1,963 scopes across 7/360 customers with no fully read-back customer. Posted the incomplete-persistence transition to Linear.
- A repeat Marriott Sandbox read-back produced no PR, confirming its profile-only state is stable. Run #34546721969 then targeted Reprise and auto-merged PR #153731: one of two expected `enableNAScore` scopes plus unrelated `enableRegexKeywordMoment` and browser-redaction changes. Production is now 8/1,963 scopes across 8/360 customers; posted the transition to Linear.
- Corrected the explicit-line read-back interpretation: `configv3` intentionally omits use-case fields equal to their profile parent, so the earlier 8/1,963 count was not inheritance-aware. Scheduled run #34547566828 and auto-merged PR #153732 added the remaining 436 profile values across exactly 352 safe-cohort customers. Combined with the prior eight profiles, all 444 profiles and their 1,519 inheriting use cases resolve true: 1,963/1,963 safe-cohort scopes. Posted the correction and transition to **configv3 effective / Admin verification pending** on Linear; the two isolated customer gaps remain.
