# Project Log - 2026-03-12

## Progress

- Rebased PR #16777 on latest `origin/main` — one conflict in `localFeatureFlags.ts` (both main and our branch added entries at end of union type).
- All CI checks passed: Lint, Typecheck, Test, Build, E2E, CodeRabbit, security scans.
- **PR #16777 merged** (squash merge).

## Details

### Conflict resolved

`localFeatureFlags.ts`: main added `enableKBCrestaAdminTools`, our branch added `enableAgentOnlyFilter` — both at the end of `LocalFeatureFlag` type. Kept both.

### Feature flag

The feature flag commit (added by user before this session) guards the "Agents only" filter behind `enableAgentOnlyFilter` on Performance page.

### Phase 4: Leaderboard filter implemented

New branch `xuanyu/convi-6247-leaderboard-filter`, PR [director #17314](https://github.com/cresta/director/pull/17314).

Changes to `useLeaderboardsFilters.tsx`:
- `listAgentOnly` added to `DefaultLeaderboardsFiltersState` Pick type, `LocalStorageLeaderboardsFiltersState`, `INITIAL_LEADERBOARDS_FILTERS_STATE` (default `true`), `localStateToFilterState` (`?? true` fallback), `filterStateToLocalState`
- BooleanFilter component with `isDisabled` on Manager tab
- `getFilterMenuOptions()`: added agents-only entry with i18n
- `FILTER_STATE_ACCESSORS`: added `listAgentOnly`
- `FILTERS_SELECTION_HOOKS`: added `useListAgentOnlyLevelSelect`
- Feature flag: `enableAgentOnlyFilter` hides filter and strips `listAgentOnly` from state
- `modifiedFiltersState` memo replaces direct `modifyFilterStateForNoTemplateValue(filters)` to support flag guard

Locale: added `leaderboards.filters.agents-only` key.

### Phase 5.1-5.3: Leaderboard API wiring

Added to same PR #17314 as second commit.

**Agent Leaderboard (`AgentLeaderboardPage.tsx`):**
- Removed `filterToAgentsOnly: true` from 3 hardcoded constants (`QA_STATS_INCLUDE_PEER_USER_STATS_OPTIONS`, `QA_STATS_WITH_AUTOFAIL_OPTIONS`, `INSIGHTS_REQUEST_PARAMS_OPTIONS_FOR_CONVOS_WITH_AA`)
- Created memoized `insightsFilterOptions` and `insightsFilterOptionsWithAA` using `filtersState.listAgentOnly`
- QA calls already use filter state via `getQAScoreStatsRequestParams` — no change needed

**Team Leaderboard (`TeamLeaderboardPage.tsx`):**
- Removed 2 hardcoded constants (`FILTER_TO_AGENTS_ONLY_OPTIONS`, `INSIGHTS_REQUEST_PARAMS_OPTIONS_FOR_CONVOS_WITH_AA`)
- Created same memoized options pattern
- Also wired `groupByTeamParams` and `groupByTeamParamsHintStats` which previously had NO `filterToAgentsOnly` at all

**Manager tab:** unchanged — no `filterToAgentsOnly` sent, correct behavior.

### Phase 6 assessment: Agent Assist — NOT a quick parallel PR

`AssistanceFiltersState` is a separate Pick type from `CommonInsightsFiltersState` that does NOT include `listAgentOnly`. Adding the filter requires:
1. Adding to `AssistanceFiltersState` Pick type + `LocalStorageAssistanceInsightsFiltersState`
2. Adding to `useAssistanceFilters` (initial state, persistence, component, menu)
3. Passing through 11+ `useInsightsRequestParams` calls across many assistance components

This is a significant scope — deferring to a separate effort.

## Next Steps

- Phase 6: Agent Assist (separate effort, not a quick parallel PR)
