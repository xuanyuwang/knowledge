# 2026-09-07

## CONVI-7638 lesson-scoped module stats — implementation (Claude Code)

Filled the empty Linear ticket [CONVI-7638](https://linear.app/cresta/issue/CONVI-7638/honor-training-lesson-names-scope-in) (title, context, scope, non-goals, acceptance criteria) and implemented the last open CONVI-7601 review follow-up (B4): `RetrieveTrainingSimulatorModuleStats` now honors `training_lesson_names`.

- New worktree `/Users/xuanyu.wang/repos/go-servers-convi-7638` on branch `convi-7638-modules-in-lesson-scope` (off `origin/main` @ `bf2376e890`).
- Change is deliberately small: parse/validate lesson names in `parseModuleStatsRequest` (mirrors module-name validation; empty list = field ignored) and add `resource_id IN lessonIDs` to the latest-revision lesson query in `findLessonsContainingModules`. Aggregation, latest-attempt selection, and response shape are untouched because facts already derive from `(task, lesson, module, agent)` assignment keys.
- `active_lesson_count` deliberately left profile-global — field 4's contract scopes assignments, not content metadata.
- Updated two tests that carried placeholder lesson names assuming the field was ignored; added parse-validation subtests and `TestRetrieveTrainingSimulatorModuleStats_LessonScopeNarrowsAssignments` (scoped vs unscoped vs no-matching-lesson).
- Focused suite green (9.6s), full package green (162s), Gazelle no-op, Bazel build green (72s). Committed `0a01e7ee95` and opened [go-servers#32039](https://github.com/cresta/go-servers/pull/32039).

Evidence: `sessions/2026-09-07/claude-convi-7638-lesson-scoped-module-stats.md`, `work-items/CONVI-7638.md`.
