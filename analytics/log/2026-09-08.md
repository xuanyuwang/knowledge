# 2026-09-08

## CONVI-7402 PR review triage

- Reviewed all general comments, reviews, and review threads on go-servers PR [#31335](https://github.com/cresta/go-servers/pull/31335) using the GitHub CLI/API.
- Identified the submit-time versus conversation-time asymmetry as the must-fix correctness issue; recommended an indexed alias/multi-group regression as a low-risk second fix.
- Recommended explicit follow-up tickets for broader Elasticsearch overlap parity and shared `parseClickhouseFilter` behavior across non-QA endpoints rather than silently broadening this already large PR.
- Recorded the completion sequence and separated already-addressed or optional CodeRabbit notes in `sessions/2026-09-08/codex-convi-7402-pr-review-actions.md`.
- Implemented the review stack as seven ordered commits: submit-time correctness, indexed alias, explicit QA scope, shared-helper fail-closed behavior, test structure, parser extraction, and shared CTE construction.
- Combined focused analyticsimpl tests passed; Gazelle was run before each commit with no BUILD changes. Pushed all seven commits to the PR branch and verified remote head `1b2a95bd9a21c2453cd70609ebd20c36f0365ed7`; CI and re-review remain.

## CONVI-7402 adversarial correctness re-review

- Reviewed the current PR head against fetched main; local rebase completed without conflicts and preserved all ten patches. No push or external review action.
- Reproduced remaining submit-time QA-conversations over-inclusion with full generated SQL: three returned rows versus the correct two counted by QA stats. Found incomplete shared multi-ID rejection, inherited numeric endpoint errors exposed by overlap, and invalid SQL from empty metadata values.
- Executed ClickHouse 26.8.2.7 locally on synthetic fixtures; ordinary overlap, latest changes, duplicates, and independent groups passed. Both QA suites and narrow parser tests passed, while a broader unit run failed the CTE extraction's whitespace-sensitive outcome assertion.
- Preserved [full review](../deliverables/convi-7402-pr-31335-adversarial-review.md) and [session/reproduction evidence](../sessions/2026-09-08/codex-convi-7402-adversarial-review.md). No product fixes; source worktree is clean at local rebased `12117463df`, remote unchanged at `1b2a95bd9a`.
- Follow-up: fix remaining contracts and durable tests in an authorized implementation pass; measure production scan cost, MV parity/freshness and distributed behavior.

## CONVI-7402 review fixes committed locally

- Implemented each of the four correctness findings in its own commit (`5970423a86`, `cd3990ac3c`, `a3efa37d07`, `1036223149`); fixed the outcome test whitespace assertion separately (`fe0109acd5`).
- Added regressions that failed before each fix and pass afterward. Combined parser/query/QA-suite/ES-parser validation passes; real local ClickHouse confirms submit-time counts align at two and numeric singleton/open/closed boundaries retain the intended rows. Gazelle diff check and source diff checks pass.
- [Session and validation evidence](../sessions/2026-09-08/codex-convi-7402-review-fixes.md). Source worktree is clean; no push or external writes. Next: user review, authorized push/CI, and production cost/MV freshness validation.

## CONVI-7402 second review

- Verified live PR head `fe0109acd5` and fetched main `782e72ba51`; no new analytics source changes on main, so no further rebase.
- Found one remaining P2 in the shared validator: omitted type on either side of a same-ID metadata overlap bypasses rejection and loses half the OR. Two failing shared-parser cases and equivalent passing ES cases reproduce it.
- Expanded checks pass for 49 fully typed ID-set pairs, all nine two-overlap value/absence combinations under both join-null settings, and four numeric-boundary cases compared with ES range objects.
- [Review and evidence](../sessions/2026-09-08/codex-convi-7402-second-review.md). Source remains clean; no fixes or external writes. Next: close the partial-type validation gap; continue CI/code-owner and production-scale checks.

## CONVI-7402 omitted-type fix pushed

- Fixed the second-review finding by classifying metadata groups before comparing all IDs; omitted moment types now correctly reject unsupported shared OR requests instead of dropping half the filter.
- Four regressions failed before the fix and passed afterward. Combined parser/query/QA/ES tests, Gazelle diff check, formatting and commit checks passed.
- Committed and normally pushed `8810713e2a`; verified exact remote/local head equality and a clean source worktree. [Evidence](../sessions/2026-09-08/codex-convi-7402-partial-type-fix.md).
- CI/code-owner review and production cost/MV freshness validation remain.

## CONVI-7402 submit-time source scan fix

- Verified and fixed the current unbounded moment-CTE scan finding with a SUBMIT_TIME-only candidate conversation restriction; preserved older conversation correctness and other time-target behavior.
- Committed and pushed `a9cf40e6c6` separately. All six source-CTE regressions, combined Go validation, local full-query/source-CTE/empty-candidate execution, Gazelle and commit checks pass.
- [Session and evidence](../sessions/2026-09-08/codex-convi-7402-submit-time-scan.md). Production read-cost/distributed performance and CI/code-owner validation remain.

## CONVI-7402 parser test placement finding skipped

- Verified the cited cases test shared parseClickhouseFilter behavior used across multiple RPCs. Existing parser tests belong in common_clickhouse_test.go; no single endpoint owns them and no shared fixture requires moving.
- Skipped the proposed relocation, preserving coverage without source churn. All 17 focused cases pass; source remains clean at a9cf40e6c6.
- [Review rationale](../sessions/2026-09-08/codex-convi-7402-parser-test-placement-review.md).

## CONVI-6719 current-behavior validation

- Revalidated the Phase 3 Linear scope and current `go-servers` user-filter implementation before any product change.
- Confirmed the remembered role-filter behavior belongs to CONVI-6930's manual-QA group-expansion helper, not `ParseUserFilterForAnalytics`: roles constrain expanded group members while explicit users remain preserved; reviewer audiences intentionally use no role filter.
- Found material plan drift: merged API is `Parser.Parse` rather than `ParseV2`; child-team expansion is already fixed; three dead arguments were removed; per-user groups are sorted; the analytics parser now has 18 direct stats callers plus scorecard reviewer audience reuse.
- Confirmed `ListUsersForAnalytics.user_ids` is implemented, while `user_types` is present in proto but not applied after canceled/reverted CONVI-7343. Canceled CONVI-7300/7301/7343 behavior is not a compatibility baseline.
- Identified the Phase 3 prerequisite: explicitly separate analytics `AgentOnly` behavior from coaching/manual-QA `Roles`/`GroupRoles`/`UserTypes`/`State` behavior and add missing role/coaching regressions before implementing the shared parser.
- No product code, branches, worktrees, commits, or Linear issues were changed. Evidence: [session](../sessions/2026-09-08/codex-convi-6719-current-behavior-validation.md) and [work item](../work-items/CONVI-6719.md).
- Design direction confirmed: the unified parser must implement `Roles` (including empty=no restriction), `GroupRoles`, `UserTypes`, and `State` as first-class filters while keeping `ListAgentOnly` semantically distinct.
- Corrected the durable unification documents: qualified the `LiteUser` mapping conclusion to output/membership construction, documented the missing eligibility-fetch strategy, narrowed Phase 1's “0 gaps” claim to the analytics baseline, added Phase 3 population-filter tests, and replaced the stale child-expansion “critical fix” with a preserve-current-behavior requirement.
