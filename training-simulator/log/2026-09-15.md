# 2026-09-15

- Refreshed CONVI-7281 against the live ticket and current `director/origin/main`. Confirmed Opera rule creation flows through `rule/create` -> `CreatePolicy` -> shared `CreateAndEditPolicy`, with the requested control in Finalize.
- Identified two Finalize implementations behind `enablePerformanceConfigSelection`; any applicability field must render consistently in both.
- Confirmed Data Management's `ApplicableAreasSelect` is the closest `ListSelect` UI precedent, while Opera's `selectedUsecases` flow is the closest store/draft lifecycle precedent. The Data Management component is semantically coupled to `MomentAppType` and should not be reused directly.
- Confirmed current Director and public policy proto have no policy-level Training Simulator/product-area applicability field.
