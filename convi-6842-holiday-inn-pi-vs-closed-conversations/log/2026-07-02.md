# 2026-07-02

- Reviewed PR comment https://github.com/cresta/director/pull/20153#issuecomment-4843907620 showing `voice-staging`, `cresta/walter-dev` before/after screenshots.
- Confirmed screenshots show `Conversation volume` changing from `8,803` to `22,574` under `Template: All`, monthly weekly filters, all minutes, and voicemail excluded.
- Verified the delta is consistent with the PR's source swap: no-template volume now uses `RetrieveQAScoreStats` with `includeNaScored: true` instead of `RetrieveConversationStats`.
- Checked backend query semantics: QA stats count distinct conversation IDs from score/scorecard tables, while conversation stats count from `message_d` joined with conversation data.
