# 2026-05-20

- Verified from code that `UpdateScorecard` returns `NOT_FOUND` from a Postgres `director.scorecards` lookup, not from a ClickHouse read path.
- Removed the alternate ClickHouse-failure theory from the knowledge doc.
- Confirmed the working model remains the Postgres read-after-write / replica visibility issue on the initial `UpdateScorecard` existence check.
