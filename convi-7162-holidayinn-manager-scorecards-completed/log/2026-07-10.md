# 2026-07-10

- Created backend worktree `/Users/xuanyu.wang/repos/go-servers-convi-7162` on branch `convi-7162-holiday-inn-club-vacations-manager-leaderboard-scorecards`.
- Implemented submit-time filtering/grouping for submitted scorecard reviewer/completion QA requests, preserving conversation-time behavior for ordinary QA scorecard requests.
- Applied the same submitted-time window to QA conversations used by the Manager scorecard drawer.
- Added focused regression coverage proving the generated scorecard-resource stats query uses `scorecard_submit_time` instead of `scorecard_time`.
- Validation: focused analytics tests and `git diff --check` pass. Full `analyticsimpl` package test was attempted but stopped after hanging with no output for several minutes.
- Investigated explicit proto-field alternative: both QA APIs already have `conversation_time_range_field` using `RetrieveClosedConversationsRequest.TargetFieldForTimeRange`, but that enum is conversation-scoped. Recommended adding a QA/scorecard-scoped field instead of extending the closed-conversations enum with scorecard submission time.
