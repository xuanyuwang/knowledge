# 2026-09-16

- Revalidated CONVI-7281 against current `go-servers`, `cresta-proto`, and `director` upstream snapshots. Confirmed the conversation-source applicability contract and runtime filter remain unimplemented.
- Traced rule application from GoWalter's `TRAINING_SIMULATOR` conversation source through orchestrator `SearchPolicies`, backend `filterDBPolicies`, Opera annotations, and Training Simulator's annotation-only evaluator.
- Confirmed the setting cannot be UI-only: persist applicability in policy-config JSONB and enforce it during policy selection before annotations are generated. No relational SQL migration is required.
- Recorded that Training Simulator now forces metadata-trigger matching, increasing the importance of runtime policy filtering.
- Refined the CONVI-7281 backend contract: reuse `Conversation.Source`; empty policy applicability means all sources and a non-empty list uses OR matching. Corrected the ticket scope to exactly two states: all conversations or Training Simulator only.
- Implemented the contract in new `cresta-proto` and `go-servers` worktrees and opened [cresta-proto#9890](https://github.com/cresta/cresta-proto/pull/9890) plus [go-servers#32384](https://github.com/cresta/go-servers/pull/32384). Source now propagates through online/cache/backtest Orchestrator paths and filters ordinary policy selection before annotation generation; explicit overrides retain existing bypass behavior.
