# Project Log - 2026-07-03

## Progress

- Evaluated a proposed redesign of `/Users/xuanyu.wang/repos` where working repositories would move under `knowledge`.
- Added protocol-compliant project metadata and a session note for workspace layout decisions.
- Implemented the chosen alternative: `/Users/xuanyu.wang/repos` is now a lightweight workspace metadata Git repo.

## Details

The main finding is that nesting working repositories under `knowledge` is possible but likely not the best default. A lean `knowledge` Git repo can ignore nested repositories, but ignored nested repos can weaken visibility from tools opened at the `knowledge` root and create confusing nested-Git behavior.

The chosen direction is to keep source repos as siblings and use `/Users/xuanyu.wang/repos` itself as a lightweight workspace metadata repository that tracks only shared instructions, manifests, and workspace files.

## Session Notes

- `sessions/2026-07-03/codex-workspace-layout-redesign.md`

## Next Steps

- Keep workspace metadata in the root repo and durable reasoning in `knowledge`.

## Related

- **Repo/worktree:** `/Users/xuanyu.wang/repos/knowledge`
- **Branch:** `main`
