# 2026-08-14

- Prepared the missing customer-facing CLO filter rollout on `xw/convi-7049-clo-production-customers`: explicitly enabled `enableCLOFilters` at every supported profile and use-case scope.
- NCLH `customers/nclh/profiles/us-east-1` on `us-east-1-prod` already had the profile flag, but the config editor identified 17 use cases where the same flag remained disabled; added those 17 entries. Final assertion: 18/18 NCLH use cases enabled.
- Added five Holiday Inn `voice-prod` profile flags and six use-case flags across `club-voice`, `owners-voice`, `social-voice` (two use cases), `transfers-voice`, and `voice`. Final assertion: 6/6 supported use cases enabled.
- Preserved `customers/holidayinn/profiles/chat` on `chat-prod` with both flags unset because the legacy `holidayinn_chat` source has no CLO trigger MV.
- Revalidated rollout prerequisites from completed ClickHouse Phase B evidence: `voice-prod` run `31220875375` validated all 24 non-empty DBs; `us-east-1-prod` run `31232485782` validated all 68 non-empty DBs with no target behind.
- Validation passed for changed-file YAML, config-history frontend schema, CI YAML parsing, exact target/exclusion structural checks, and diff whitespace. Full aggregate wrappers were limited by `E2BIG` and a missing local `run-p`; no dependency install was attempted because package-registry credentials were not cleared.
- Impact: the proposed config makes the Performance Insights CLO selector consistently available at profile and use-case scopes for NCLH and the five supported Holiday Inn voice profiles while retaining their already-enabled optimized MV reads, with no unrelated customer edits.
- Follow-up: review config#151643 and let CI complete; do not merge or deploy from this session.
- Rebased onto current config `master`, committed as `89ed8cf800`, pushed `xw/convi-7049-clo-production-customers`, and opened ready-for-review [config#151643](https://github.com/cresta/config/pull/151643). No merge or deployment performed.
- Validated all PR comments and automated precedents. Current CI is green and GitHub reports clean/mergeable. Exact MV/CLO scope parity is 1 profile + 18 use cases for NCLH and 5 profiles + 6 use cases for Holiday Inn; chat remains excluded. CodeRabbit's schema-directive request is a false positive because customer-history YAML has no per-file directives (0/367), the automated predecessor PRs used the same format, and schema/lint/config-consistency checks pass.
- Session: `sessions/2026-08-14/codex-production-customer-enablement.md`.
