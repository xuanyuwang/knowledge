# Project Log - 2026-03-20

## Progress

- Bug fix PR #26417 merged and deployed to voice-staging
- Re-tested ReindexScorecards workflow on walter-dev with both paths:
  1. **Write path** (clean_up_before_write=false): Deleted CH data, triggered via cron with `REINDEX_MODE=process` — 196 scorecards + 721 scores written, all 4 validations pass
  2. **Cleanup path** (clean_up_before_write=true): Triggered via cron with `REINDEX_SCORECARDS_CLEAN_UP_BEFORE_WRITE=true` — delete + re-write succeeded, same 196/721 counts, idempotent as expected

## Details

### Test 1: Write path (no cleanup)
```
Job: reindex-process-test-1774022818
Image: main-20260320_150304z-b64c0eca (includes PR #26417 fix)
Result: 196 scorecards, 721 scores — all validations PASS
```

### Test 2: Cleanup path (delete before write)
```
Job: reindex-process-cleanup-1774023077
Env: REINDEX_SCORECARDS_CLEAN_UP_BEFORE_WRITE=true
Result: 196 scorecards, 721 scores — all validations PASS
```

Both bugs confirmed fixed:
- Bug 1: `deleteFromCH` now correctly targets local tables (`score`, `scorecard`) with `ON CLUSTER 'conversations'`
- Bug 2: `CleanUpBeforeWrite` is now configurable via env var (defaults to `false`)

### Remaining
- Prod rollout
