# 2026-08-10

- Ran post-merge production verification for CONVI-7162 on Holiday Inn `transfers-voice` Manager Leaderboard.
- Initial flag-off UI for Cliff Hawker, Jun 15–21, showed 2, 3, 1, 2, 0, 2, 0 (10 total), matching ClickHouse `scorecard_time` exactly.
- Corrected the production check with `?filterByScorecardSubmitTime=true`: for Aug 4–10, the UI showed 2 on Aug 4, 5, 6, and 10 (8 total), exactly matching ClickHouse `scorecard_submit_time`; `scorecard_time` returned only 6. The guarded release is present and working, while default rollout configuration remains off.
- Repeated the flag-on check on the official non-beta `/director/` route. It showed only 6 (Aug 5, 6, and 10), matching `scorecard_time`; the URL retained the flag parameter. Result: `director-beta` has the working fix, but the official Director release does not yet honor it.
- Detailed evidence: `sessions/2026-08-10/codex-release-verification.md`.
- Traced the staging release after all three PRs merged. Director staging successfully deployed `main-ea98741` via flux-deployments PR #316770, and the backend insights-server release also succeeded.
- Confirmed the rollout gap: config PR #150988 declared `filterByScorecardSubmitTime`, but current staging config does not enable it for `cresta/walter-dev` (or any staging config). The deployed UI therefore evaluates the flag false and omits `timeRangeFilterTarget`.
- Detailed staging evidence: `sessions/2026-08-10/cursor-staging-release-investigation.md`.
